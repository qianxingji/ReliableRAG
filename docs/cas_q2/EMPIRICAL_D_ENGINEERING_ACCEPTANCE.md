# Client Lead acceptance: D outcome and analysis engineering

CAS Q2 STATUS: NOT READY. Effective 2026-09-11.

Accept committed source 4249598d975cdc15649b2fcee029603806bbc5b3 for CPU
engineering only. EMPIRICAL_D_EXECUTION_CONTRACT.md was committed first at
21724e7, before implementation or fresh Gold access. No actual D mapping,
analysis or fresh performance result exists. The complete C4 prelabel gate is
still pending accepted C3 generation and scoring.

The value-blind original-source inventory passed for 785 files / 407,507,969
bytes, including the three reference sources, original selected-reference and
metric definitions, interpreter and 770 original Arrow package files. It read
metadata and opaque hashes only; fresh Gold values and canonical answers decoded
were zero. Inventory manifest:
13a9e14d8f58c737dd1aef2f8556bb9bf1a6d81a86acdd910acdeecde9732ef2.
This is source availability/integrity, not metric replay on the new population.

The complete regression ran 134 tests: 132 passed, with two existing Windows
filesystem capability skips. All 1,054 frozen inputs remained byte-identical.
The 24 new D tests cover original/independent EM and F1, special answers and
aliases, selected-span JSON/JSONL and real Arrow on invented references,
answer-only canonical projection, nine-policy global/cell estimates, weighted
allocation against independent explicit copies, sibling-cluster RNG, binary
storage/partial failure/corruption rejection, all six interval directions and
method-blind/numeric-only boundaries. Missing or incomplete C4 predecessors are
rejected before output creation. All four real CLI entrypoints import correctly.
Engineering manifest:
9461d2d4dd54c6935eb9dac949b381c6bd3489857b8b4a6ddbb1e2864e34e0c8.

No new test failure occurred in this final regression. The earlier C4 test
assertion failure and all previous failed namespaces remain preserved. New D
inventory and engineering private ZIPs were exported and every entry was
verified against its sealed bytes. No new scientific fit occurred; total 178.
Version any necessary correction to these now-frozen source bytes.

Before actual controllers were written, review identified an overly broad
potential decode grant when an entire authenticated predecessor graph was
passed to the inventory guard. That audit never read fresh labels, and no D
mapper had run. EMPIRICAL_D_DECODE_BOUNDARY_REFINEMENT.md records the prospective
correction: the immutable inventory guard remains, while actual controllers
layer an exact decoded-file allowlist with empirical_outcome_guard_v2.py.
Tests show an authenticated score sidecar can be hashed but not parsed by a
mapper. This changes no scientific rule or prior result.

After complete C4 acceptance, execute these separate processes in order, each
with the exact accepted predecessor manifest arguments shown by --help:

1. scripts.map_roa_empirical_outcomes: original selected-reference readers and
   metric formulas; 18,000 seven-field numeric rows, reference binding and
   access receipts; no policy decode or raw reference strings in outputs.
2. scripts.validate_roa_empirical_outcomes: selected-reference reread and
   independent metric formulas; all 72,000 scalar values, complete identities
   and siblings; exact EM and existing 1e-15 F1 bound, then numeric seal.
3. scripts.analyze_roa_empirical_results: numeric-only frozen analysis, all
   nine policies, global actions in all cells, 20,000 paired question-cluster
   draws and fixed-action sensitivity, 240,000,000 count bytes and full receipts.
4. scripts.validate_roa_empirical_analysis: independent point/cell formulas,
   every regenerated RNG multiplicity and every explicit-copy allocation,
   all integer events, intervals/directions and complete source/input rechecks.

These test gates do not establish real full-process success. The formal run
must preserve partial files on failure and cannot retry into an existing
namespace. The independent mapper shares authenticated original source parsers;
the analysis validator shares pinned NumPy RNG/quantiles. Intervals condition on
fixed models and realized bounded pools. Computational PASS does not establish
equivalence, a novel contribution or submission readiness.

The snapshot in EMPIRICAL_D_ENGINEERING_RESULTS.json is 07:32:22 UTC: C3 had
5,099 complete pairs and 15,298 generation receipts. Counts are sequential and
non-atomic, with no answer-quality assessment. All 41 frozen C3 source/config
files remained unchanged. Continue that original process; do not duplicate it.

P0: complete C3, fixed 180-trace replay and independent acceptance; execute C4
GPU preflight/base/GbV/policies/full prelabel validation; then all four D stages
and honest contribution review. Principal rejection risks remain failed
historical advancement rules, no supported novel-method claim, incomplete fresh
evidence and seven original per-estimator fit-time ID/matrix receipt gaps.
P1: full inference cost, external-comparison fidelity, contamination boundaries
and reproducible release. P2: no new model/feature/seed/budget search. CAS year,
institutional category rule and target journal remain user-undecided.
