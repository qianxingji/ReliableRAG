# Client Lead acceptance: protected C4 stage engineering

CAS Q2 STATUS: NOT READY. Effective 2026-09-11.

Accept source commit 61edb9a5983155bc16f1a6f0e5907e731b4aea3a for engineering
implementation and invented-input CPU tests only. The execution contract was
committed first at f96228d. No actual C4 GPU preflight, fresh scoring or complete
prelabel acceptance occurred. Both historical advancement failures remain.

The protected executors and separate complete file-level validator now exist.
They check accepted C3 canonical/replay/independent receipts before creating any
stage namespace. They authenticate all predecessor source/input freezes and
require a clean committed checkout. Each stage uses a new output/cache and
rejects raw data, fit, generation, retrieval, network, subprocess and model loads
outside its declared scope. CPU policy/validation stages deny neural forwards.
GPU preflight denies benchmark ledger decoding. The Python audit/profile guard
is a process-level check, not a machine-wide security claim.

All 36,000 answer strings are embedded before eligibility. Base and GbV scores
retain every one of the 18,000 traces. Forward observations and completed NLI
branches are flushed/fsynced separately; counters include attempted failed
forwards. Compact score records contain explicit forward references. The
independent validator reconstructs C3 evidence bindings separately, consumes
every reference once, checks token/cache/chunk/vector observations, rebuilds
native features and saved-head arithmetic, and verifies all nine policies and
the common global K=900 budget. A prelabel seal requires this full gate.

Namespace: empirical_scoring_stage_engineering_v1. Manifest:
4164666e8ad38360a3980393cd569a00476c4a196dc601db8a896aa37477017e.
The full suite ran 110 tests: 108 passed and two existing Windows filesystem
capability cases were skipped. All 243 frozen inputs remained unchanged. Nine
new tests cover complete durable file roundtrip with original scorer methods
and real saved heads, known second-branch failures, nonfinite failed-forward
accounting, corrupt/missing/duplicate/orphan references, strict prerequisite
chains and all five stage-specific read/forward boundaries. Neural logits are
invented CPU fixtures; these tests load no real pretrained neural model.

An initial development assertion expected four cache hits instead of six. The
third invented pair reuses its two a0 cells as well as the four-cell repeated
pair. Only the fixture expectation was corrected; scientific/cache behavior was
unchanged. DEVELOPMENT_TEST_FAILURE.json preserves this failed assertion and
its explanation. Full final logs, freeze and results are sealed; a private ZIP
was byte-checked after export. No failed scientific namespace was retried.

The observed scope limitations remain: likelihood checks saved target-token
log probabilities, not full-vocabulary softmax; semantic checks saved-vector
dot products, not BGE internals; NLI checks saved logits and the prospectively
fixed FP32 softmax bound, not a second NLI forward. Tokenizers and HGB sklearn
probabilities are shared authenticated dependencies. The 1e-10 head gate and
exact observed scores/masks/actions are unchanged.

Use EMPIRICAL_C4_STAGE_ENGINEERING_RESULTS.json for the timestamped C3 snapshot.
At 06:37:45 UTC it contained 3,762 complete candidate pairs and 11,286 generation
receipts; these non-atomic byte counts are not completion. All 41 frozen C3
source/config files remained unchanged. The original process continues.

Next P0: complete C3, its fixed 180-trace replay and independent acceptance;
then execute preflight_roa_empirical_scoring_gpu, build_roa_empirical_scoring
(base, gbv, policies in order), and validate_roa_empirical_scoring with explicit
accepted predecessor manifest arguments. Inspect single-use namespaces before
launching. Do not edit the newly CPU-frozen source bytes; version any necessary
correction and preserve failures. In parallel with C3's remaining runtime,
implement the already frozen D analysis without reading fresh Gold. Gold
mapping/outcome evaluation may run only after complete prelabel acceptance.

Principal rejection risks remain unestablished contribution, incomplete fresh
empirical evidence and original per-estimator fit-time receipt gaps. Method
novelty is not cleared; baseline scores and complete inference costs still need
real execution; the experiment and argument cannot advance on CPU tests alone.
P1: baseline fidelity, full cost, contamination boundaries and reproducible
release. P2: no new model/feature/seed/budget search. Total takeover fits remain
178; CAS year, institutional rule and target journal remain user-undecided.
