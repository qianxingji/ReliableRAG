# Postlabel C4 V2 path binding

The first actual cost invocation failed before creating its scientific namespace
because the frozen outcome prerequisite helper still resolved the C4 GPU
preflight path to the preserved V1 failure while the accepted final C4 chain
correctly pins the V2 manifest. No generation receipt, Gold value, model or
policy payload was decoded. The external failure is sealed with manifest
`f49f8c76e8e6c9ca8158f9824484a721b80eae4616c5e17cf5e8df3e0dee0120`.

`scripts.run_roa_empirical_postlabel_bound_v2` is a prospective versioned entry
for the cost and four D processes. A byte-hashed config binds the exact stage,
unchanged target entry, clean source commit, original arguments, final prelabel
manifest and both preserved V1 and accepted V2 preflight manifests. It verifies
the original five C4 paths, changes only `gpu_preflight` to the V2 namespace in
memory, and then invokes the unchanged target. The adapter and config are added
to every new scientific executable freeze.

It does not change a cohort, result, action, score, reference, metric, seed,
endpoint or tolerance. It never edits V1, V2 or the complete C4 seal. Five tests
cover the only-path change, exact target set, changed-control rejection,
byte-exact config hash and cross-stage entry rejection.

**CAS Q2 STATUS: NOT READY.** The corrected entry must pass its tests and then
the cost and D stages must execute and receive separate client acceptance.
