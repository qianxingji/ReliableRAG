# D decoded-input boundary refinement before execution

CAS Q2 STATUS: NOT READY. Effective 2026-09-11.

The completed outcome-input inventory used empirical_outcome_guard.py in its
metadata-only audit mode. Preserve that source and all audit records unchanged.
No new reference labels, predictions or outcomes were decoded in that audit.

Before implementing actual D controllers, code review found that an
authenticated predecessor graph can include base/GbV score sidecars and other
historical numeric files. The first guard blocks the final policy namespace
from mapping and blocks raw/canonical text from analysis, but a mapper supplied
the entire graph as its read allowlist would still have more decode permissions
than needed. No mapper has run, so this is a prospective execution hardening,
not a corrected scientific result or a claim that the inventory read labels.

Use a new empirical_outcome_guard_v2.py for actual mapping and analysis. Layer
an exact decoded-file allowlist over the original boundary. Every decoded file
must already be authenticated; other predecessor files may only pass through
the exact opaque digest helper. Mapping/validation decode only the selected IDs,
canonical answer projection, authorized selected-reference sources and their
necessary code/metadata, plus the numeric ledger when validating it. Analysis
decodes only accepted numeric outcomes and frozen prelabel ledger inputs and
necessary metadata. No broad predecessor-hash list serves as a decode grant.

Require invented-file subprocess tests demonstrating that an explicitly
authenticated score sidecar can be hashed but cannot be parsed by a mapper.
Preserve all previous denials. No cohort, model, metric, score, action, seed,
statistical endpoint or numerical tolerance changes. Actual D still requires
complete C4 prelabel acceptance, separate executable freezes and validation.
