# C3 V2 correction: helper constants and preserved failed report

Research Lead design, 2026-09-12 Asia/Shanghai, before V2 implementation.
**CAS Q2 STATUS: NOT READY.** This prospective correction follows the
[actual failure review](C3_VALIDATION_V1_FAILURE_REVIEW.md). It changes no
scientific model, data, algorithm, comparison, threshold, seed or sample.

## Frozen work and exact correction

Keep the original validator, original helper source, all 41 C3 source/config
pins and all four accepted V1 adapter/fixture sources unchanged. Preserve the
completed canonical and fixed neural replay byte-for-byte; do not repeat either.
Their manifests remain 4df45c4c68d5961198ab7ecf2857a19efb0d15e1990aad9a3a7dc85b627415c4
and 0597b34a5b0a51b146d6ffd53002b0c96c65579929e22353cb8fbafbb87e1c2d.

Add separate V2 binding, entry and fixture modules. Reuse the unchanged V1 length
and finalization behavior, with explicit V2 metadata. The original loader still
creates the original helper functions. In their shared dictionary, add exactly:

- DATASETS = ('hotpotqa', '2wikimultihopqa', 'musique')
- RETRIEVERS = ('bm25', 'dense', 'hybrid')

Before adding either, authenticate current/original source bytes and literal
assignment ASTs, require both tuples to agree, and require both names absent
from the freshly loaded helper scope. Reject preexisting values, reordered or
changed tuples, substituted source bytes, multiple scopes or unresolved names.
Inspect global-name dependencies recursively in every original helper code
object and nested code object. The missing set must be exactly these two names
before binding and empty afterward. Record the full dependency inventory.
Preserve all other scope entries/identities and original function code objects;
the only additional behavior beyond V1 is this constant binding. Do not import
the original historical executor or runtime_support with a star import.

Continue to use original main, validate_all, guard, helper bodies, namespace
check, every comparison and original failure handling. Retain V1's exact
fixed-tokenizer length observation, periodic vocabulary checks and full control
inclusion in the successful final seal. The production entry must authenticate
the actual V2 source set, prior fixture/client evidence and new V2 fixture
evidence before reading current payloads; do not reuse a V1 PASS to authorize
untested V2 code. All bindings must be explicit in the new execution receipt.

## Required invented-only regression gate

Run in a fresh single-use fixture directory with original authenticated native
packages, CPU one thread, no CUDA initialization, neural model/forward, fitting,
current payload/Gold reads or raw benchmark reads. Freeze code/commands/inputs
before execution and preserve all unexpected failures.

Call actual functions returned by the original loader. A well-formed invented
branch must reproduce NameError with neither constant; adding only DATASETS
must expose the RETRIEVERS NameError. With both correctly bound, exercise all
nine allowed strata, empty answers retained, and each original branch rejection:
extra/missing field, wrong string type, invalid dataset/retriever, wrong passage
count/type and nested forbidden field checking. Compare a separately constructed
scope using authenticated original constant-assignment ASTs to the V2 adapter;
require identical returns or exact original rejection reasons.

Add negative tests for changed tuples/source pins/prebound names and unresolved
nested-code globals. Verify no other original helper has an unresolved global.
Check the composed V2 load/length/result/finalization path: use actual tokenizer
assets and invented a0, repair_query and a1 receipts with branch validation
before generation validation. Reconcile observed generated IDs and retain the
V1 length criteria. Verify original failed status stays FAIL, metadata-only
binding errors fail before first result creation, existing outputs refuse
overwrite, and original verify_namespace covers all final control bytes.
The successful original scientific result fields must keep their canonical hash.
These tests add the missing integration coverage; they are not a full current
benchmark execution or permission to repeat the already accepted V1 suite.

## Preserve and relocate the single failed result

The V1 failed original report currently occupies the standard result path.
The unchanged validator requires that path absent and the initial runtime file
set equal exactly the two original acquisition namespaces. Do not weaken either
check or hide the V1 failure inside a fabricated success receipt.

Before any mutation, authenticate the exact V1 failure seal, failed report SHA
ca90d4d03eca6f6d1620aa9c0c03c9d821dc498c815b3bc5e2a11d7d642442ce
/ 5,505 bytes, and all fourteen original runtime files. Confirm no writer is
live, no prior V2 run exists, and no SEAL/final SHA manifest/validation controls
exist. Prepare a dedicated external failure archive containing an exact report
copy, complete V1 attempt files, static audit and archival operation controls;
independently rehash all archive members and verify its complete manifest.

Only after verified archival preservation, move the exact failed report from
outputs/cas_q2/empirical_runtime_v1/INDEPENDENT_VALIDATION.json to the new sibling
outputs/cas_q2/empirical_runtime_validation_failures_v1/v1/INDEPENDENT_VALIDATION.json.
Resolve both absolute paths, require the exact expected source and a nonexistent
destination, reject reparse/symlink paths, and use one native same-volume move
without overwrite. No recursive delete, reset, cleanup or removal of the V1
attempt directory is allowed. This is an explicitly disclosed archival move of
one unsealed failed result, not modification of an original sealed payload.

Independently rehash the moved original and archived copy; write an exclusive
relocation receipt with paths, before/after hash/size, archive manifest pin and
proof that the runtime namespace is again exactly its fourteen original files.
Seal that sibling failure directory independently. Keep all external failure
seals outside the current runtime namespace. Bind their pins and operation
receipt in the V2 configuration and successful final execution controls.
On any precondition or archival failure, retain everything and do not launch.
If interrupted, inspect actual state; never blindly retry a move or full run.

## Complete V2 execution and final acceptance

After V2 fixture acceptance and verified archival move, launch one V2 complete
validator in a new task outputs/c3_bound_validation_v2 namespace. Require a clean
committed checkout and freeze the exact command, original interpreter and source
pins, V1/V2 evidence, original canonical/replay manifests, environment inventory
and relocation seals. Use the original native environment, one CPU thread,
offline settings and no model/fit/forward/CUDA. Hash all 30,823 original package
files, every original runtime/source input and all new controls before/after.

Validate every one of the 18,000 canonical plus 180 replay traces, all repair
rankings, original counter/failure/stratum rules and exact replay signatures.
Require 54,540 successful generation checks, actual generated-ID count equal to
specialized length calls, zero failed generation checks and 38 complete
vocabulary rechecks. No new expected scientific hash or tolerance is introduced.
An unexpected error stops this V2 pass, retains partial artifacts and seals a
new failure. It cannot be accepted as an exclusion or trigger automatic retries.

The successful standard runtime result, acquisition seal and final full manifest
must include all V2 controls and failure-provenance pins. Final client acceptance
must independently verify every original result field, acquisition/replay pins,
scope/length observations, preservation evidence and exact final namespace, then
execute the unchanged C4 stage_prerequisites('gpu_preflight', {'runtime': ...})
against the actual final hash. Only that final acceptance permits C4. A fixture
PASS or compatibility inference is insufficient.

Sol High implements and executes this correction; Astra xhigh performs the
final authenticity/integration audit. The correction is disclosed execution
adaptation, not literal unchanged CLI replay or independent neural/model
implementation. No additional fitting, current Gold decoding or contribution
claim is authorized; fits remain 185. P0 remains complete fresh empirical
evidence; P1 full delivery/provenance; P2 no additional search.
