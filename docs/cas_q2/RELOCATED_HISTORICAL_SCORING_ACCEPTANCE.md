# Client Lead acceptance: full relocated historical scoring

2026-09-11. **CAS Q2 STATUS: NOT READY.**

The complete historical CPU feature, saved-estimator and HGB/V2 decision path
passes from the previously restored roots in the separately installed neural
package environment. Both historical advancement failures remain unchanged;
this result establishes reproducibility, not method superiority or readiness.

## Coverage and independent validation

The frozen scope covers all 13,500 historical traces / 4,500 questions, including
all nine 1,500-trace strata. The producer reconstructs every one of the 3,202
eligible feature pairs using the original restore/feature functions and archived
semantic values and four likelihood cells. It loads the seven original mars_full
estimators and original five-fold V2 bundle through their original class identities,
then calls unchanged base-score and V2 scoring functions. All feature fields and
32,020 base-score values, 3,202 V2 values and 27,000 archived actions match.

All 10,298 ineligible traces are preserved: 10,296 normalized-equal pairs and two
empty repaired answers. Their 102,980 null base-score values stay null. The
global action budget remains K=675 of N=13,500, with the original lexical tie rule.
There is no redraw, imputation change, fitting or calibration.

| Actual verification | Coverage | Maximum error |
|---|---:|---:|
| Producer numeric comparisons | 236,948 | 0 |
| Separate standard-library validator numeric comparisons | 240,150 | 0 |
| Separate validator exact-value comparisons | 196,788 | No mismatch |
| Archived HGB/V2 actions | 27,000 | All identical |
| Independently derived HGB/V2 top-K decisions | 27,000 | All identical |

The independent validator authenticates its inputs against the accepted static
graph, reads every producer row and archived reference, and derives rankings
itself. It imports no producer/scientific implementation and loads no estimator.
This is a full archived-reference comparison; it is not an independent tree
implementation or another neural forward. The declared 1e-12 tolerance is unchanged.
The five current empirical parameter heads were loaded as metadata and checked
by the original saved_models function; fresh panel predictions were not tested.

All 69 used original inputs and their 69 restored copies remain unchanged.
All 21,267 installed environment/target files and eight control files remain
unchanged before/after. Both child processes report zero old-project content
reads, fresh/historical Gold values, fits and neural forwards. The producer loads
exactly eight historical saved objects; the independent validator loads zero.
No Torch/Transformers or raw dataset module is imported. Original metadata and
scientific source bytes remain intact. Total takeover scientific fits remain 178.

## Audited execution and evidence

Design commit: `b168e0b`; execution source:
`39e11ed26f54d512b1905b2848f42d507f467d7e`.
The unchanged v2 source adapter binds eight v3-support Path globals. The original
FIELDS assignment and saved_models function AST are compiled into an explicit
module with only disclosed load/require/PANEL bindings; all scientific checks and
bodies are retained. Original 34 base/V2 AST records match prior accepted records.
The original bundle_check AST verifies its complete saved configuration.

The producer and independent process both exit zero. Complete per-row feature
results, final replay rows, empty mismatch reports, exact commands, guards and
controls are retained in the new private namespace. Acceptance completed at
2026-09-11T12:10:26.317401+00:00:
`PASS_FULL_RELOCATED_HISTORICAL_SCORING`.

Acceptance manifest:
`6066a29364a1555a27bebdbdd3efa147f4188f1d2f918457a660b9493c90d1e7`.
Execution freeze:
`7ee3060a2bc87c037f0ae0d2127563a6ddfcc51a4bba03c5eac159053be84ecf`.
Final replay (20,220,137 bytes):
`50ebddb1a1f742b3c5f2be32859c8440bc37b73e6cc30cec946d2c6b1ae60042`.
Independent report:
`06877a6ef38a7be5519360b99223dafa3e035a6d447124c66f24f8df95d69851`.
See [machine-readable results](RELOCATED_HISTORICAL_SCORING_RESULTS.json) for
private-kit digests and the latest C3 snapshot. No private artifact is uploaded.
Do not repeat accepted historical replay, assembly, environment or transport runs.

## Windows metadata discrepancy

The producer recorded platform.uname version 10.0.26100; the earlier assembly
recorded 10.0.26200. Both original records are preserved. A separate two-process,
OS-metadata-only diagnostic reused the same authenticated guard and interpreter:
default process creation returned cmd's 10.0.26200; CREATE_NO_WINDOW reproduced
a UnicodeDecodeError in CPython's unmodified _syscmd_ver UTF-8 text decoding.
Because UnicodeDecodeError subclasses ValueError, win32_ver used its existing
fallback to sys.getwindowsversion().platform_version = (10, 0, 26100).
The diagnostic changed no OS, scientific code, outputs, encoding or guard rules.
It records both paths, zero denials/scientific calls and unchanged source controls.
Diagnostic seal:
`5a1974d07e010f7b0c80d3a74a98b2cb71f49af0b1a6b065562d04edb5d32595`.
Do not claim identical platform metadata or rewrite the successful replay receipt.

## Remaining work and rejection risks

This reconstructs historical features from archived neural quantities and replays
saved CPU estimators. It does not reproduce the neural quantities, reconstruct
the original training, perform fresh outcome analysis, establish a novel-method
contribution, validate another host/OS, or run the complete original C4/D CLIs
from arbitrary roots. Seven historical per-estimator fit-time ID/matrix receipts
and the earlier independent full mars_full manifest pin remain missing.

- P0: complete the original C3 canonical process, fixed 180-trace replay and full
  independent acceptance; execute C4/full prelabel, cost and four D processes,
  then review contribution against the frozen scientific design.
- P1: prospectively verify remaining package/predecessor bindings and completed
  empirical stage dependencies, then complete relocated neural/pipeline execution.
- P2: no model/feature/seed/budget search. CAS year, institutional rule and journal
  remain user-undecided and do not block the current experiments.

Principal rejection risks are unestablished contribution after both failed
historical gates, incomplete fresh results and training-provenance limits.
HGB remains an input/comparator; fixed HGB_GBV_R is not established novelty.
