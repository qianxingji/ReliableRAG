# Client Lead acceptance: declared static dependency delivery

2026-09-11. **CAS Q2 STATUS: NOT READY.**

The declared static dependency graph and its actual three-root restoration pass.
This closes byte transport for the audited data, learned-policy, source and
already sealed output files. It does not accept C3 completion or full neural
pipeline replay. No original scientific implementation or artifact was changed.

## Independent evidence

The prospectively frozen 21 seeds resolve to 17,617 distinct files and 21,118
dependency edges. All hashes and declared sizes match; there are no unresolved
records, missing files or conflicting declarations. All 36 applicable namespace
file sets match their seals. Only 74 explicitly approved metadata carriers were
decoded; the other 17,543 files remained opaque. The active C3 launch freeze is
a newly observed static input, not an independently completed runtime namespace.

Graph authority, from source `d33ed6050d087c116e74aaa775cad586337715bf`:
`aa5c3da7ed764405e92602b64303984931a798abf2aee3e64c5d56ae34e892ea`.
The graph contains 11,840,356,271 bytes. Its 22 pretrained files are routed to the
separately accepted asset archive; all remaining 17,595 files / 4,345,359,738
bytes are in a new private stored ZIP64 archive with 15 audit/control members.

The independent validator authenticates the sealed graph and supports, checks
every node/edge declaration, verifies all nodes are reachable from the exact
21 seeds, and derives archive membership without importing the writer or auditor.
It restores all graph files from the two archives, preserving three logical roots:

| Root | Files |
|---|---:|
| Original project | 16,975 |
| Engineering worktree | 636 |
| Task workspace | 6 |
| Total | 17,617 |

Actual fresh restoration: `E:/paper/ReliableRAG-static-roots-v1`, containing
`original`, `engineering`, and `task` subdirectories. Its exact file set and
all hashes pass. The writer separately rehashes all 17,617 originals and all
17,617 restored files after the independent process; every file and all 15
support files remain unchanged. Both archives retain their byte digests.

The independent process reports zero original-project content reads, blocked
events, scientific fits, model loads, neural forwards and fresh Gold decodes.
The writer permits exactly its one declared validator process. The earlier
151-test scientific engineering suite and accepted package/assembly tests were
not repeated; this gate uses actual full archive/graph/restoration validation.

Delivery source: `3e8cabd8c8e83d1c7105974e1a4ca7782bf8f0ac`.
Completed 2026-09-11T11:48:15.128225+00:00 with status
`PASS_DECLARED_STATIC_TRANSPORT_AND_RESTORATION_ONLY`.
Delivery acceptance manifest:
`54b44ed473665264a366ee0bb24e5a205b43c0bb012799fdd654592c620a4ff6`.
Independent receipt:
`fbd0d232ed22f2574f6aa0d071eaaad67ea75f5f78086014e1728c028393a41b`.

Use [results and private release digests](STATIC_DELIVERY_RESULTS.json) and the
[offline runbook](STATIC_DELIVERY_OFFLINE_RUNBOOK.md). The small evidence companion
supplies exact controls and freeze bytes; Git line endings are not an alternative
byte witness. The large packages and all private graph/payload data remain local.
Keep the accepted restoration and earlier archives sealed; do not repeat this gate.

## Remaining scientific and execution gaps

The graph proves the listed static declarations, not absence of undeclared IO
in arbitrary code. Original metadata still contains original absolute paths.
C4's native root/package checks and C3/C4/D's predecessor-path checks still need
a separate explicit delivery binding, preserving logical identity and old bytes.
The excluded live C3 ledgers and actual future C4/cost/D outputs need later
completion seals and a separate delivery extension. No inference or scientific
controller was launched from the restored roots; CUDA, complete relocated
execution and another-host/OS reproduction remain unverified.

Both historical method advancement rules remain failed. HGB is an upstream
input/comparator; the fixed fusion policy has no cleared novelty claim. Fresh
quality/cost analysis remains pending. Seven historical per-estimator fit-time
ID/matrix receipts and the earlier independent full mars_full manifest pin are
still missing. File delivery cannot reconstruct these historical witnesses.
ID exclusion is not semantic/pretraining decontamination. A 5% action cap is not
a 5% compute budget; unsaved timing/peak-memory fields remain explicitly missing.

- P0: finish the original C3 canonical process, fixed 180-trace replay and full
  independent acceptance; then actual C4/full prelabel, cost and four D processes,
  followed by contribution review against the frozen design.
- P1: prospectively bind the restored learned objects, package roles and
  predecessor records; later close completed-stage dependencies and execute the
  full relocated pipeline with honest provenance and comparison limits.
- P2: no extra model/feature/seed/budget search. CAS year, institutional category
  rule and journal remain user-undecided; this does not block the experiments.

Principal rejection risks remain unestablished contribution, incomplete fresh
empirical evidence and historical training-provenance gaps. No Submission Ready,
final-model or confirmation approval is issued by this transport acceptance.
