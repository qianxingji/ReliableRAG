# Read-only diagnostic of the retained HGB byte mismatch

Research Lead design, 2026-09-11. CAS Q2 STATUS: NOT READY.

The seven-fit replay is sealed as a failure under manifest
e85024fc8e697cf9f69220d90c85a944027f14a9cef3c7624f463ff75981cc84.
All seven original final fits completed. Six model files are byte-identical;
HGB differs. Independent comparison of all 10,773 development scores and the
producer's scores has maximum error zero. Its three counterfeit checks pass.
All 13 original/restored inputs, 21,267 environment files and eight controls
remain unchanged. There have now been 185 actual scientific fit attempts and
185 completions in the takeover, including seven historical reproduction fits.

Original HGB SHA-256:
9245170f855435b5603b04013bd4fbab79a75d2761853a012ab9f3259600bf6e.
New HGB SHA-256:
88b75fd0c63eacea9f118453fc4b2f81c118b29a54dda7234f011605129f1545.
Neither pin, model, failed receipt nor acceptance requirement may be changed.

Before implementation, authorize this separate diagnostic only: load those two
authenticated HGB artifacts in one isolated CPU process under the original
selected class definitions and accepted numerical packages. Load no dataset,
label, prediction ledger or other model. Permit zero fits and zero scoring,
prediction or neural forward calls. Keep the prior failed stage immutable.

Record file sizes, total differing byte positions and bounded first offsets.
Compare the complete reachable serialized state using explicit recursive
handling of primitives, dictionaries, sequences, NumPy scalars/dtypes/arrays
and source/sklearn objects' state. Record every object class, every key/shape/
dtype and every semantic scalar. For structured arrays, inspect every named
field and retain whole-buffer hashes separately, so unnamed padding bytes are
not silently conflated with learned node values. For objects with no dictionary,
record their class and __getstate__ result; unsupported or recursive state is
an unresolved diagnostic finding, never silently omitted.

Save both complete state observations and all differing paths/values. Use exact
semantic comparison, with explicit representations for nonfinite metadata;
do not introduce a tolerance or alter arrays/models. If serialized attributes
differ, identify their actual source usage in the installed authenticated
sklearn sources before interpreting the difference. If only padding or another
serialization detail differs, explain the evidence while preserving the failed
literal-byte gate. No replacement dump, re-fit, different thread setting or
repeated score pass is authorized by this diagnostic.

Rehash both original/new artifacts, selected sources, all environment files
and controls before/after. Save every warning/error and seal a separate output.
This diagnostic cannot upgrade the original replay to PASS, replace the seven
missing fit-time receipts or establish agreement on unseen inputs. It can
narrow the explanation for the observed difference and guide later review.
P0 stays on original C3/C4/D stages; P1 addresses this discrepancy and remaining
neural/predecessor delivery. P2 adds no research search. Fresh Gold remains closed.
