# Client Lead acceptance: invented downstream scoring integration

CAS Q2 STATUS: NOT READY. Effective 2026-09-11.

Accept empirical_scoring_integration_cpu_v1, executed from commit
d67abf27414d9efaa8c08703683447b532934383, for invented-input downstream
integration only. Its immutable manifest is
e1020b95bde5079517dfb94a37c073aaa55be746fff730aeffb66d9945c3e4a4.
The complete executable freeze authenticates the preceding accepted CPU
compatibility run, original models/definitions and all new integration code.
No original or previously frozen file was edited.

The guarded run used 36 invented traces spanning all nine dataset/retriever
strata. Semantic values, likelihoods and NLI probabilities were explicitly static
fixtures. Seven real saved upstream estimators, the saved V2 ensemble and five
fixed parameter heads were loaded and scored, with zero fits, neural-model loads,
neural forwards, fresh branch reads or Gold reads. Original native eligibility
retained 18 eligible pairs; one invented deterministic NLI failure left 17 under
the common mask. All 36 stayed in N_all and the eight scored policies each used
the same two-action cap; Keep made zero replacements.

Independent evidence-derived feature reconstruction and saved-head arithmetic
passed 1,637 numeric comparisons. Maximum error was 4.440892098500626e-16, below
the unchanged 1e-10 threshold. All nine action sets, masks, reason counts and
coverage passed exact checks. Reopening the written JSON/JSONL files produced
the identical independent report; this is not counted as additional distinct
scientific evidence. HGB checks still share saved sklearn estimator probability
calls and do not independently traverse trees. Actual neural witnesses have
not yet been validated in this run.

Implemented adapters now bind exact accepted C3 schemas/hashes and evidence
metadata before calling the original trace restoration; record document-mode
answer embeddings and token batches; call the unchanged likelihood/feature/GbV
paths; preserve partial NLI work on documented deterministic preparation failure;
and reconcile the common mask before fixed-policy scores and global allocation.
Raw base scores remain intact when a later NLI failure forces every policy to
keep. Unknown errors, post-forward ValueErrors, malformed inputs and missing or
nonfinite eligible scores stop execution rather than creating new exclusions.
Full protected neural executors and their independent witness checks are still
required; these adapters are not an authorization to score unfinished C3 rows.

Six added integration tests cover evidence/hash/schema corruption, native
semantic encoding including empty/equal answers and a partial final batch,
complete saved-model integration, feature/score/mask/action/coverage corruption,
NLI preparation-versus-forward failure, missing-score rejection and the all-keep
case. The first test attempt exposed an incorrect test assertion that assumed
input order equaled the canonical sorted ledger; lookup was corrected by trace
identity. No scoring output, algorithm, threshold or frozen experiment changed.
The tool transcript retains that failure. Full regression then passed 86 of 88
tests, with two previously known Windows filesystem-capability skips.

C3 canonical generation continues in its existing process and namespace. Its
completion, fixed 180-trace replay and full independent validation remain P0.
Next scoring P0 is the pinned-model GPU preflight, full neural witness validators
and protected execution integration, followed by the complete prelabel seal and
separate frozen outcome analysis. No fresh performance claim exists; total
scientific fits remain 178 and both historical advancement failures stand.

The principal rejection risk is still an unestablished research contribution,
with incomplete fresh empirical evidence and historical per-estimator training
receipt gaps. Baseline fidelity, full inference costs and release reproducibility
remain P1; no model/feature/seed/budget search is added under P2. CAS partition
year, institutional category rule and target journal remain undecided by the user.
