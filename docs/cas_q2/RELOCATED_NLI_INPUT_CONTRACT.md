# Full historical NLI preparation and SentencePiece target binding

Research Lead design, 2026-09-11. CAS Q2 STATUS: NOT READY.

## Historical oracle and exact scope

The archived prelabel GbV provenance declares 13,500 traces: 3,202 scored pairs,
10,296 normalized-equal pairs and two a1-empty pairs. Its score ledger records
both premise and chunk counts. It does not contain original input token arrays,
per-premise chunk strings or neural logits. Do not manufacture those historical
witnesses or claim historical NLI probability replay from new input preparation.

Authenticate the accepted static graph and actual restoration, unchanged neural
environment/target file list, original source and original prelabel seals before
execution. Freeze every selected file, controller, command and output namespace
before decoding historical per-row payloads. Historical GbV provenance is
b3ba2717fcbe783ccf1b33e254759c060d82e2cac4b7357909c52ed4fbf15079;
its complete score ledger is
c4cc54d7ec3e6d8d4663f57ecbc1ff52e15b3448bf0f9cee76404f03a49e42ca.
Use the original 13,500 canonical branches from runtime_branch_freeze. No new
cohort, redraw, seed, scoring model, training, action selection or Gold is allowed.

Run the full historical universe, retaining every ineligible pair and its reason.
On every eligible pair, reconstruct both branches (6,404 branch preparations;
32,020 original premise passages before any splitting). Match every archived
eligible/reason/premise-count/chunk-count field exactly; no numerical tolerance
or discarded unscorable rows. Unexpected preparation exceptions must be saved
and fail acceptance against the original zero-unscorable provenance. Historical
F0/F1/margin values may be decoded solely to bind the archived row hash; they are
not used, recomputed or accepted as replayed probabilities. No outcome/Gold file
is opened. The current empirical C3 ledgers remain excluded.

The archived execution/gbv.json records 6,404 observed forward calls and 32,174
scored NLI pairs. Authenticate that metadata too and reconcile the newly prepared
batch/pair totals to those historical observations. The new processes still make
zero forwards; matching these counts does not reproduce logits or probabilities.

## Native and independent preparation

Use the already accepted new neural environment plus exactly its separate
SentencePiece 0.2.1 target. Do not inject PyArrow or reuse the old environment.
Bind the physical restored five non-weight NLI tokenizer/configuration assets
to MoritzLaurer/deberta-v3-large-zeroshot-v2.0 revision
5a4338ab2151dc8db04ad53b42b6153382bf4f99. Call AutoTokenizer.from_pretrained
with use_fast=False, local_files_only=True and trust_remote_code=False. Load
AutoConfig only, with no neural model constructor. This is disclosed path and
factory orchestration, not a claim that the full original scorer runs on CPU.
Record actual DebertaV2Tokenizer type, model_max_length=512, entailment index=0,
the SentencePiece module/native binary paths and all package versions.

The producer selects unchanged original gbv_nli.py constants/functions for the
hypothesis, effective context length, entailment index, pair length and
split_passage_to_fit. Reuse the original answer-eligibility function. Preserve
the exact quoted hypothesis template, passage order, 20-word overlap, binary
search/chunk boundaries and no-truncation semantics. Group each branch's ordered
chunk/hypothesis pairs into batches of eight. Prepare CPU tokenizer tensors with
the original truncation=False, padding=True, return_tensors='pt' arguments,
then save every input ID, attention mask and any native token-type IDs. Save
all chunk strings, hypothesis, batch boundaries and source-row hashes. These
are new preparation witnesses, explicitly dated to this run.

A separate process must not import the producer or native splitting function.
Select the already frozen empirical_gbv_independent.py hypothesis/length/chunks/
positive_index functions (source hash
209f73ccc7bc2943ba6a97dc8a789ed3fb25404ccbe3b6756cc0a770b0d77507),
whose chunk routine independently verifies complete word coverage. Reconstruct
every branch with its own slow tokenizer, then compare every new hypothesis,
chunk, batch boundary and token/mask/type array exactly. For the independent
batch comparison use ordinary lists, checking consistency with the producer's
CPU tensors. Both processes share the same fixed SentencePiece implementation;
this is independent source/orchestration, not two independent tokenizers.
Reconcile the full 13,500-row universe, order, nine 1,500-row strata, 4,500 question
groups, all historical counts/reasons and new token totals. Compare the full
tokenizer vocabulary hash before/after. Do not cache or alter original chunk
functions, monkeypatch tokenizer methods or replace neural scoring with a stub.

## Failure boundaries and preservation

Before full decoding, use bounded invented fixtures to compare both original
chunk routines: a short premise, a genuinely overlength premise, an impossible
hypothesis, and malformed entailment labels. Require complete word coverage,
nontruncated emitted pairs and the correct failure reason. Keep fixtures separate
from historical counts. They cannot substitute for the full historical run.

Use one CPU thread, CUDA_VISIBLE_DEVICES=-1, offline settings and the accepted
guard's narrow Windows/urllib3 bootstrap behavior. A separately frozen NLI guard
may add only the exact new SentencePiece target as an allowed library root.
Keep model/estimator loading, neural forwards, fits, network/process actions,
current empirical/raw-data reads and external writes forbidden. Verify loaded
SentencePiece Python and native modules actually belong to that target.

The library's original long-sequence warning is expected during deliberately
untruncated length probes, before splitting. Preserve stderr. Accept only that
specific single warning per tokenizer, with a reported length greater than 512
and authenticated tokenization_utils_base source; every other stderr message
or unexpected audit/profile event fails. No overlength batch may reach a model
or be saved as valid prepared input. No model exists in this gate.

Rehash all used original/restored inputs, all 21,267 new environment/target files
and controls before/after; retain full partial output and all failures. Never
modify old hashes, sources, outputs or acceptance conditions to obtain a pass.
The accepted static restoration and earlier Qwen gate remain sealed and are
not repeated. Original C3 continues on its original GPU/process without changes.

Acceptance is limited to full historical NLI preparation/count consistency,
independent new input witnesses and actual SentencePiece target binding. BGE,
PyArrow outcome bindings, actual NLI probabilities/CUDA, C4/D predecessor execution,
full relocated neural pipeline and another-host/OS reproduction remain pending.
P0 is C3/replay/acceptance, actual C4, cost/D and contribution review. P1 continues
complete delivery and provenance. P2 adds no parameter/model/seed/budget search.
The novel contribution, fresh empirical results and seven fit-time receipts remain
major missing evidence; this gate cannot imply CAS Q2 Submission Ready.
