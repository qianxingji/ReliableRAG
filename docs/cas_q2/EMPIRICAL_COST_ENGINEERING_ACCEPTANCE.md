# Client Lead acceptance: inference cost engineering and component fidelity

CAS Q2 STATUS: NOT READY. Effective 2026-09-11.

Accept source e311a24a547a62c0726a6d146b697bc26ecd9e6e for cost-engineering
preparation only, following prospective contract de4bb42. The final full suite
ran 141 tests: 139 passed, two existing Windows filesystem capability skips.
All 1,065 frozen input files were rechecked unchanged. No new test failure
occurred. New engineering manifest:
304ba81f0cf28a64ea8fb03b8412cce2e85e419d4adef33d5cce75b98553d252.

The seven cost tests use invented generation and scoring receipts. They cover
answer/passages skipped before text materialization, complete stage/stratum
accounting, corrupt/duplicate/missing/out-of-order records, likelihood cache
closure, retained NLI first-branch work after second-branch failure, hybrid
component counts, explicit missing measurements and protected access gates.
The cost namespace has not executed on fresh inputs. CPU PASS cannot substitute
for complete accepted C3/C4 receipt reconciliation.

After complete prelabel acceptance, scripts.audit_roa_empirical_cost requires
--project-root and --prelabel-manifest-sha256. It authenticates all predecessors
before creating empirical_inference_cost_v1; only accepted metadata and the
canonical/replay generation cost projections are decodable. It performs no
Gold read, model load, generation, retrieval or fit. Runtime answer strings and
scoring payloads are not cost inputs. Full ledgers and all C4 witness/counter
closures must pass; failures retain their exclusive namespace.

Canonical shared inference and verification overhead are reported separately.
Stage timings keep their different boundaries; no summed end-to-end latency or
standalone policy speedup is claimed. Canonical C2/C3 GPU allocator peaks and
per-generation latency were not saved. C2/C3 BGE token totals, per-forward
generation token touches and FLOPs are also unavailable. Report these missing
measurements instead of changing running/frozen sources or rerunning inference.

The source-level comparator review is
[GBV_SOURCE_FIDELITY_REVIEW.md](GBV_SOURCE_FIDELITY_REVIEW.md). It verifies the
published component recipe against unchanged local source while explicitly
identifying paired margin, global budget, dataset/reader and outcome-metric
adaptations. The old phrase “published paired adaptation” does not establish
published authorship of our paired policy or author-code reproduction. No policy
identifier, scientific protocol, parameter, score or budget was changed.

The private cost-engineering ZIP was exported and every entry matched its sealed
bytes. D inventory/engineering ZIPs and all earlier archives remain preserved.
See EMPIRICAL_COST_ENGINEERING_RESULTS.json for the snapshot at 07:51:19 UTC:
5,556 complete canonical pairs, 16,669 generation receipts and 41 unchanged C3
source/config files. Sequential byte counts are non-atomic and not completion.
The original C3 process remains active; never duplicate it.

P0: finish C3, its fixed 180-trace replay and independent acceptance; execute C4
and full prelabel validation; then four D processes and honest contribution
review. P1: execute cost reconciliation, enforce comparator claim boundaries
and prepare reproducible release. P2: no model/feature/seed/budget search.
Both historical advancement failures remain. Main rejection risks are absent
novel-method evidence, incomplete fresh experiments and seven original model
fit-time receipt gaps. CAS year/institutional rule/journal remain undecided;
total scientific fits remain 178 and fresh Gold reads remain zero.
