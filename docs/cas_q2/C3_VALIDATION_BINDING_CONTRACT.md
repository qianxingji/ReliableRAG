# Frozen execution binding for complete current C3 validation

Research Lead design, 2026-09-11, before implementation or current validation.
CAS Q2 STATUS: NOT READY. This implements the execution priority in
[the source observation](C3_VALIDATION_EXECUTION_NOTE.md); it changes no method,
cohort, generator, neural replay, metric or acceptance threshold.

## Preconditions and unchanged original work

Keep the original canonical process and its 41 frozen source/config files
unchanged. Finish all 18,000 canonical traces and the existing fixed 180-trace
neural replay. The following adapter may validate current payloads only after
both original BUILD_RECEIPT / EXECUTION_MANIFEST chains pass their existing
checks. Before then, engineering runs only invented receipts and metadata.
Do not read current answers, output tokens, scores or Gold for adapter design.

Preserve the original C3 validator bytes
`3eb99ad55f93af8beaa0d316d8892ca8747a627bb80671d358312b758638915a`
and the original imported independent helper source
`12634390df5c76ae30baeb3172778a86fdae87b7ffc06491db4ab57fcb83fd0a`.
Call the original current validator's main, validate_all, reference/ranking
helpers and guard. Retain every original full namespace, input, source,
generation, repair, counter, stratum, replay and byte check. No sampled substitute
or second interpretation of a failed comparison is authorized.

## Exact length binding and observations

Use a separate entry module and helper; do not edit original files or builtins.
Wrap the original load_independent_functions only to obtain its unchanged
functions, verify all selected helper ASTs, and bind `len` in that one original
helper namespace. A small wrapper around its original validate_generation
observes its actual tokenizer argument, then calls that original function.
It does not implement generation checks or replace their return/failure result.

Bind exactly the first fixed Qwen2TokenizerFast instance passed by the unchanged
current validator. Require the original class/module identity, actual installed
source location and authenticated assets, with original pinned Qwen revision
`aa8e72537993ba99e69dfaafa59ed015b17504d1`. Before its first receipt, require
ordinary builtin len 151665, complete get_vocab canonical hash
`54a4e00eec5a8c5f40d137afdde38da75f4c46ffa802b509cb5fb2157b5a5b0d`
(UTF-8, sorted keys, ensure_ascii=False, compact JSON, allow_nan=False), and chat
template hash `cd8e9439f0570856fd70470bf8889ebd8b5d1107207f67a5efb46e342330527f`.
Only that object's len returns the measured cardinality. Every other object
uses ordinary builtin len, including its usual TypeError. Reject a replacement
tokenizer in any subsequent generation call. Do not mutate the fixed tokenizer.

Recheck ordinary length, the full vocabulary hash and chat-template hash after
each 1,500 successful generation validations and at completion. Count every
specialized length call and reconcile it to every generated token ID in all
successful receipts. Require 54,540 successful generation checks (54,000
canonical plus 540 replay). Record actual IDs checked; no output-token count is
estimated from previous cohorts. Preserve all empty answers/parser fallbacks
under the original rules. Unexpected failure stops this pass and remains saved.

## Execution provenance and the existing C4 chain

The separate driver authenticates and preloads its exact source/config/contract
and accepted fixture evidence before the original guard. Freeze the command,
environment, original source pins and declared output paths before launch.
Use the original native environment, one CPU thread, offline settings, no model
load/forward, fit or CUDA initialization. Do not alter the original guard.

Two explicitly disclosed finalization bindings add provenance without replacing
original results or seals. Intercept the original write_json only for its first
INDEPENDENT_VALIDATION.json creation: retain all original scientific fields and
add execution_binding metadata. On an adapter invariant failure set the shared
result to FAIL before that first write, preserving the actual reason; never
upgrade an original failure. Other writes call original write_json unchanged.

Intercept the final seal only to exclusively create the preloaded execution
controls under runtime/validation_execution after the original full namespace
check, then call the original seal function. The new controls and binding
metadata must therefore be covered by the final SHA256_MANIFEST consumed by C4.
They must not be written into the runtime directory before the original
canonical/replay namespace check. No existing file is overwritten, no expected
hash is changed, and a partial success without the complete final seal is not
accepted. The original computation-boundary snapshot does not claim to cover
these later finalization writes; the adapter records their declared paths and
client review checks the actual final manifest and exact control bytes.

Keep original C4 stage_prerequisites unchanged. Its verify_namespace must cover
the added controls, its original statuses/counts/replay/zero-Gold checks must
pass, and all 41 original source/config files must still match. The client must
verify this chain explicitly before starting C4. Do not turn compatibility with
these checks into a claim of literal unchanged original CLI execution.

## Required preflight and final acceptance

Use actual authenticated tokenizer assets with invented five-document evidence,
questions and receipts for a0, a1 and repair_query. Run the identical original
validate_generation with ordinary len and with the proposed binding; require
matching successful results and exact rejection reasons for changed prompt,
input IDs/mask, invalid generated IDs (out-of-range, negative, bool), empty or
over-budget output, wrong stop token/decode/counters/render/config. Verify
ordinary len behavior on other objects, exact tokenizer identity, full
vocabulary/cardinality checks and rejection of a replaced tokenizer.

For the vocabulary-mutation rejection only, use a separate in-memory tokenizer
clone with an invented added token. It may never become the fixed runtime
tokenizer or be saved. Original assets, packages and the primary fixture
tokenizer remain unchanged. Exercise finalization hooks in a separate invented
runtime namespace, including failure preservation, no-overwrite refusal,
control-byte inclusion and the original verify_namespace over the final seal.
These are fixtures, not current runtime validation or neural replay.

Freeze and test the implementation before actual current validation. Preserve
any failure and make corrections prospectively. Client acceptance requires all
fixtures, exact source/asset/environment preservation, no unexpected stderr,
complete original current C3 independent checks, all 54,540 generation
observations, exact length-call/token coverage, periodic full-vocabulary checks,
recorded execution bindings and the complete downstream predecessor chain.
Historical tokenizer acceptance alone is insufficient. Total scientific fits
remain 185; current Gold remains closed. Next P0 is full C3 acceptance, then
actual C4/full prelabel and cost/D; broader BGE/neural delivery remains P1.
