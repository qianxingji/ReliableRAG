# Audit and deliver the declared static dependency closure

Research Lead contract, 2026-09-11. CAS Q2 STATUS: NOT READY.

The original replay, neural package environment, native definition assembly and
22 pretrained assets have accepted, separate delivery evidence. Complete the
remaining data, learned-policy and sealed-output dependencies without reading
scientific payload values or presenting the live C3 namespace as complete.

## Dependency authority

Start from the already accepted manifests for fixed panel A, cohort B, C1
projection/pool, C2 GPU/canonical, C3 CPU/preparation/GPU, C4 engineering, D source
audit/engineering, cost engineering, and the original supervised controls.
Preserve C1's failed v1 namespace as an explicitly failed root. Include the
accepted acquisition/scoring audits and original upstream provenance report.
Record every exact anchor and digest in an immutable seed list before traversal.

The active C3 EXECUTABLE_FREEZE.json may be included only as static launch
metadata, after checking its mode, expected trace count, source commit and two
accepted predecessor pins. Its current byte digest is a new observation, not a
retrospective independent seal. Exclude all live C3 ledgers and future stages
from the deliverable; list their missing completion/replay/acceptance receipts.

Interpret only declared metadata schemas, after authenticating their bytes:

- Namespace SHA256_MANIFEST.json and EXECUTION_MANIFEST.json `files` records.
  Original-project manifests use original-project-relative paths; engineering
  manifests use their containing namespace for relative payload paths. Absolute
  records retain their declared identity. Reject escape/alias/unknown roots.
- Executable, preselection, protocol, trace-preparation and CPU-test freezes:
  `inputs`, `input_files` and `source_files` lists; protocol `files` are source
  records relative to their owning project. Do not omit A/B's distinct fields.
- The three accepted acquisition/scoring/outcome audits: `checked_files`.
- The accepted upstream provenance report: its 14 manifest records, relative to
  the original project. Keep its historical provenance gaps unchanged.
- Native runtime config freezes: `accepted_sources`, `preflight_sources`.
  Native preflight metadata: `accepted_source_files`, `source_byte_inputs` and
  `historical_source_authentication.sources`. Implementation provenance:
  `sources`, `evidence_controls`. Cohort freezes: `source_id_ledgers` and
  `selected_id_ledger`. These are the additional explicit record locations used
  by the inspected original/native input-authentication code, not permission
  to decode arbitrary configuration or outcome payloads.

Resolve the original project, engineering worktree and task workspace as three
separate logical roots. Deduplicate identical file identities while retaining
every dependency edge. If the same logical file has conflicting declarations,
report a conflict; do not pick a convenient hash or rewrite metadata. Recursively
follow only the listed authenticated metadata types, never arbitrary strings,
unknown JSON schemas, payload directories or filesystem search fallbacks.
Report any unsupported reachable declaration as an unresolved edge.

Raw JSON/JSONL, Parquet/Arrow, scores, predictions, outcomes, caches, tensors and
serialized estimators are opaque payloads. Hash/copy bytes only. In particular,
do not decode even one new Gold value, unpickle models, import scientific
packages or call an existing acquisition/scoring/evaluation entry point. There
is no model fitting, inference, cohort selection or output analysis in this gate.

## Audit, delivery and limitations

First produce the full declared graph, metadata-resolution decisions, physical
availability, exact digest/size checks, conflicts/missing edges and byte totals.
Check actual namespace file sets where the accepted seal specifies completeness.
Preserve all failed runs and files found through those sealed manifests. Do not
hide a gap behind an inventory PASS. The graph is evidence for declared static
dependencies, not a proof that arbitrary source code has no undeclared IO.

Only a gap-free declared graph can be used to build its new private transport
package. Represent already delivered pretrained assets as explicit external
members tied to their exact existing archive/lock; do not redownload or repackage
those weights. Include every remaining graph payload/control file and preserve
the three-root structure, original metadata bytes and source versions. Historical
package/launcher witnesses are not a substitute for the accepted fresh runtime.

Use a separate validator to authenticate the package against the audited graph,
check every member, actually restore to fresh roots, and rehash all restored and
original files. No overwrite, hash replacement, decoder, network or GPU use is
allowed. Keep any failed archive/restoration. Seal the audit and delivery in new
namespaces. Their acceptance remains separate from original full numerical or
neural replay and cannot advance the C3/C4/D execution gates.

P0 is unchanged: original C3 completion, fixed replay and independent acceptance,
then C4/full prelabel, cost/D and contribution review. P1 still requires explicit
remaining package/predecessor bindings and complete relocated execution after
the original stages pass. Full fresh results, contribution evidence, seven
historical fit-time receipts and another-host validation are not supplied by
this static closure. P2 adds no model/feature/seed/budget search.
