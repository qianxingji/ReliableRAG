# C3 V3: fixed native-default validation after complete arithmetic census

Research Lead prospective design, Astra xhigh, 2026-09-12 Asia/Shanghai.
**CAS Q2 STATUS: NOT READY.** This contract is frozen before V3 implementation,
fixtures or execution. It permits one full non-neural validation only after all
gates below pass. It does not authorize C4.

## Basis and narrow correction

Keep the canonical 18,000-trace generation, fixed 180-trace neural replay,
scientific sources, original helper bodies, data, prompts, models, seeds,
thresholds, rankings and expected hashes unchanged. Never repeat either neural
pass. Preserve both V1 and V2 full-validator failures.

The accepted complete census establishes that the authenticated current-native
NumPy 2.2.6 / OpenBLAS 0.3.29 SkylakeX pthreads DLL, with
`OMP_NUM_THREADS`, `MKL_NUM_THREADS` and `OPENBLAS_NUM_THREADS` absent and a
single observed pool of 12 threads, exactly reconstructs every saved repair
field. One-thread V2 differs on 507 rows but changes no membership, order or
replacement. Historical acquisition did not record its actual thread count.

V3 changes only the declared validation execution environment from explicit
one thread to the one global current-native-default mode. This is a prospective
reproduction mode supported by complete saved-field evidence, not proof of the
historical thread setting. Do not search other thread counts, choose a mode by
row/dataset, add tolerance, round values or change an expected score/hash.

## Preserve V2 and restore the initial namespace

The V2 failed report SHA-256
`1979588de22cc5b450acdffb0f2231500fb70e61e2e9e612d776f91574f7c36b`
currently remains at the standard runtime path. Before any move, authenticate
the complete V2 failure task manifest
`2642f6f0e1df4f0987e5ab43f4c7e470f479e09dc1fe23062ea36f131d20fa88`,
the accepted census/audit manifests, the V1 sibling seal and all fourteen
original acquisition files. Require no live writer, no V3 task, no standard
SEAL/final manifest/validation controls and a clean committed checkout.

Create a complete external V2 archive and independently reopen and rehash every
member. Then make exactly one no-overwrite, same-volume move of the unchanged
failed report to a new sibling
`outputs/cas_q2/empirical_runtime_validation_failures_v1/v2/INDEPENDENT_VALIDATION.json`.
Write an exclusive relocation receipt and sibling failure seal. The existing V1
sibling is immutable. Require the standard runtime namespace to be exactly its
fourteen original files afterward. Any failure stops before V3 and is preserved;
never retry the move blindly or edit either failed report.

## V3 implementation and invented-only gate

Create separate V3 binding, entry and fixture files. Reuse the accepted V2
constant binding and V1 fixed-tokenizer/finalization binding without changing
their bodies. The V3 entry must authenticate all inherited controls and the
complete census/client audit before payload access. It must remove, not set,
the three thread override variables; load NumPy/threadpoolctl before numerical
validation; and require at start and end exactly one OpenBLAS pool with:

- 12 threads, version 0.3.29, SkylakeX, pthreads;
- the exact authenticated DLL path and SHA-256 used by the census;
- NumPy 2.2.6 and the original native package environment.

Profile and fail closed on model/encoder forward, CUDA initialization and fit.
Keep offline settings and use the authenticated digest boundary for all opaque
package files. Record actual package paths and threadpool state. No raw benchmark,
current Gold, learned-policy artifact or outcome ledger may be read.

Before current payload access, run a new single-use invented-only gate in two
separate processes. The accepted default-mode fixture must exercise all inherited
V2 branch, helper-scope, tokenizer-length, generation and finalization cases and
must observe the exact pool identity above. Negative fixtures must reject each
thread override being present, wrong/multiple pools, wrong thread count/version/
architecture/layer/path/DLL bytes, changed census/audit pin, preexisting result,
and any forbidden model/fit/CUDA call. Use invented arrays to demonstrate that
the V3 adapter does not introduce tolerance or replace original comparison
semantics. Preserve every failed fixture attempt.

Sol High implements and runs the fixtures. An independent client review must
verify the tested V3 source hashes, all inherited source identities, exact
rejection reasons, empty forbidden-event ledger, environment before/after and
private fixture archive before the one full run is permitted.

## One full V3 validation

After fixture acceptance, launch exactly one V3 full validator in a fresh
external task namespace. Freeze source, command, environment, inputs, prior
failures, census/audit pins and relocation evidence before execution. Rehash all
30,823 original environment files and every original/current control before and
after. The standard runtime namespace must be the restored fourteen-file state.

Invoke the unchanged original `main` and `validate_all` through the disclosed
V3 entry/bindings. Validate all 18,000 canonical and 180 replay traces, every
branch/provenance/generation/repair check, original counters, strata and exact
replay signatures. Require 54,540 successful generation checks, zero generation
failures, the exact generated-ID/length-call reconciliation and 38 complete
vocabulary rechecks. The arithmetic comparisons remain literal equality.

On success, the original finalization may create the standard independent report,
acquisition seal, validation-control directory and complete runtime manifest.
Those outputs must include V1/V2/V3 controls, both preserved failure seals,
census/audit records, execution-environment evidence and the original result
before-binding hash. On any mismatch or exception, retain the new literal V3
failure at its actual path, seal the external task and stop. No automatic V4,
retry, tolerance change, output cleanup or C4 launch is allowed.

## Final acceptance boundary

Astra xhigh performs the final authenticity/integration audit. Independently
verify every original result field, complete namespace coverage, source/input
immutability, threadpool identity at both boundaries, 180 replay signatures,
failure preservation and zero forbidden calls. Then execute the unchanged
`stage_prerequisites('gpu_preflight', {'runtime': <actual-final-manifest-sha>})`
against the actual standard runtime manifest. Only a passing client audit and
that real prerequisite check accept C3 and authorize C4.

The accepted wording must disclose historical-thread uncertainty and the V2
one-thread failure. It may claim complete current-native-default repair and full
receipt validation only if V3 actually passes. It may not claim independent
neural regeneration, answer quality or historical 12-thread provenance. Fits
remain 185 and current Gold remains closed.
