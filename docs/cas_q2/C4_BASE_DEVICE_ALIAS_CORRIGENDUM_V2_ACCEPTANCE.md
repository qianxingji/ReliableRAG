# C4 base scoring and device-alias corrigendum V2 acceptance

Date: 2026-09-12. Client Research Lead review: Sol High after Astra xhigh
authenticity decision. CAS Q2 STATUS: **NOT READY**.

Decision: **ACCEPTED_C4_BASE_AND_EXACT_DEVICE_ALIAS_CORRIGENDUM_V2**. The sealed
18,000-trace base stage is accepted within its saved-witness scope, and the
single frozen GbV stage is authorized through
`scripts.run_roa_empirical_scoring_bound_v3` with exact runtime, V2 preflight
and base predecessor manifests. No GbV result, Gold or quality evidence exists
yet.

The base scientific namespace is sealed under manifest
`6cbc051be274617e26f548f4b51b5a6ec3cddeb5e1caf6761f00fa8301913cab`.
It completed all 18,000 traces, including 4,267 native-eligible pairs and
17,068 likelihood requests. BGE made 2,250 forwards over 36,000 answer rows;
Qwen made 16,932 forwards with 136 current-run cache hits. Fit, answer
generation, new retrieval and fresh Gold counters are zero.

The ordinary independent CPU audit reconstructs all canonical identities,
semantic token/vector bindings, likelihood token preparation, cache chronology,
four-cell reductions, feature pairs and saved-head scores. It performs 311,491
numeric checks with maximum error `1.27675647831893e-15`, rehashes 476 frozen
inputs and 30,912 environment files, and seals under task manifest
`8f240e2d950f021533e02903aa5d4bcd63687a8413887abaa04bfbea4f1e6522`.
Its first audit-controller attempt failed on an audit-only tuple-key JSON
serialization and remains preserved under manifest
`ae8cea154ceccf22779ad486d1735c414a88ca7f048358c6349cb405ee091674`;
the scientific stage was not repeated.

The Astra xhigh final authenticity audit accepts the base evidence and proves
that the observed `cuda` metadata comes from the frozen sources'
`torch.device("cuda")`, while the pre-forward hook records every actual tensor
on `cuda:0`. Its manifest is
`aeb30f8d7251d134a445c4a3e6fbfeb3760c16231f423e1c3881797eaee4fc13`.
It permits only a separately named, prospective and hash-bound correction.

The implemented V2 correction accepts only the exact base manifest, receipt,
source commit, `cuda` metadata, `["cuda:0"]` forward-device lists and complete
BGE/Qwen counters. It never rewrites the receipt and does not provide generic
CUDA alias acceptance. The original validator and V2 bound entry remain at
hashes `0f040b1e3ef76484b1bea9edf3235009044bf13655ff5326748a765709bdc91d`
and `dd77f2cbed8f0dee123facb859685a9a0b84b3bc33d4d9f94aec35ce16edd3b2`.

The committed producer gate verifies all 132 empirical tests, including 15
fail-closed alias corruptions, and rehashes the full environment. Producer task
manifest: `6e6c6122f0f10be71d55b9bcc8adde085fe843b35b9f5ff774dc8d5cc4d82e63`.
The separate client audit parses all 132 raw successes, reruns the seven new
tests, independently reconstructs the real base binding, rehashes 30,912
environment files and reopens every producer-archive member. Client manifest:
`f988d39075bbf3823322d7532dc3f43725645aa7b868cd4c8473c94a43bc0719`.

Next, create a clean commit-bound V3 execution config and external controller,
verify the GbV namespace is absent and every predecessor remains sealed, then
run the one permitted full GbV stage. Preserve any failure and independently
audit its NLI tokens, logits, branches, counters and manifests before policies.

P0 remains GbV, policies, complete independent prelabel validation, cost and D.
P1 remains training provenance, cost and release fidelity. P2 remains final
candidate, confirmation design and journal qualification. The base audit does
not independently replay BGE/Qwen full neural forwards and makes no quality,
fairness, contribution or Submission Ready claim.
