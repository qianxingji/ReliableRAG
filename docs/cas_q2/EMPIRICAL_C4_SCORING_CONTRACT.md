# Research Lead contract: fixed scoring and prelabel actions

CAS Q2 STATUS: NOT READY. Effective 2026-09-11 before fresh scoring.

This implements section C of EMPIRICAL_REPLICATION_PROTOCOL_V1.md after C3
candidate-pair acquisition passes its canonical, bounded-replay and full
independent checks. It does not change the five fitted policies, cohort,
budgets, endpoints or bootstrap design. Engineering and read-only asset checks
may proceed while C3 runs. Do not score an incomplete branch ledger or inspect
generated answers to optimize this design. This document is not the complete
executable input freeze or permission to open Gold.

The scoring availability audit at commit 2735eba passed: 73 files and
880,110,248 bytes, including seven original upstream estimators, the sealed V2
bundle, the five-head parameter artifact, the pinned GbV snapshot, its 17
SentencePiece package files, native source controls and configuration. Manifest:
1027d9e0c062c11bca3c6427d2c2db6403b42e25880d34777d5e32b6adae8ebc.
It loaded no model and read no fresh branch. Qwen/BGE snapshots are additionally
bound by the earlier accepted acquisition inventory; their likelihood/semantic
scoring paths require their own preflight. This audit does not recover the
missing original per-estimator fit-time receipts.

## C4a. Native feature acquisition

Use the authenticated original prelabel_seal_v3/native_base.py definition
assembly and original reader_likelihood.py; adapt paths and 13,500/4,500 counts
to 18,000/6,000 only. Do not execute the historical omnibus fitting/evaluation
orchestration. Preserve selected AST bodies and module class identities needed
to deserialize the seven saved estimators. Authenticate their bytes before
loading and prohibit fit/partial_fit throughout this stage.

Reconstruct each trace from the accepted method-independent branch row and
its exact evidence provenance. All five rank/title/document ID/content hash/
retrieval score bindings must match C3; no defaults, fuzzy joins or regenerated
metadata. Keep the original historical feature schema and metadata-only split
field if required by the native builder; it is not an outcome or predictor.

Run the native BGE answer-semantic function on every pair before eligibility
filtering. Use canonical order within each dataset and flat a0,a1 text order,
encode_documents without the query prefix, BF16 CLS-normalized vectors,
512-token cap and batch size 16. There are 36,000 answer texts and 2,250 forwards
planned (12,000 texts / 750 batches in each dataset). Preserve empty strings and
equal pairs. Save embeddings and their exact trace/text/batch bindings so the
independent validator can recompute every paired dot product. This is answer
embedding cost, distinct from original/repair retrieval embedding cost.

Apply the original answer-pair eligibility rule. For each eligible pair compute
the original four teacher-forced likelihood cells L00, L01, L10, L11 in their
native order. Use the pinned Qwen BF16 model, seed 20260828, batch size 1,
16,000-character renderer, baseline prompt/chat template, answer-token-only
likelihood and max_length 8192 with native left prompt truncation. These are
scoring limits, not new generation limits. Use a new initially empty likelihood
cache; retain native content-key reuse within this run. Record every requested
cell, hit/miss, prompt/answer/evidence bindings and token counts. Require four
requests per pair and exact requests = computed sequences + cache hits.

Save token-level likelihood witnesses in sidecars without changing the native
scoring return values or reductions. Preserve native sum/mean/min arithmetic,
feature construction, matrix order, preprocessing and seven model scores. Record
the remaining B_rule, higher_own_likelihood and likelihood_margin definitions
exactly. No answer generation, document/query retrieval, new fitting or labels
are available. Fresh per-cell times/counters remain operational records, not
features or exact replay fields.

## C4b. Native paired GbV

Use the authenticated corrected gbv_nli.py, original hypothesis template and
branch-by-branch score loop. Pin DeBERTa revision
5a4338ab2151dc8db04ad53b42b6153382bf4f99, float32, batch size 8, slow tokenizer,
local assets only and positive entailment index 0. Resolve the index through the
unaltered corrected resolver, never an override. Respect the tokenizer/model
effective context window, fit-checked passage chunks and 20-word overlap;
no scientific truncation. F0 and F1 are the maximum entailment probabilities
over each branch's chunks; paired score is F1 minus F0.

Save exact chunk/premise/hypothesis/token bindings, per-batch logits and resolved
class probabilities with instrumentation that leaves the native outputs intact.
Independently verify chunk coverage, token limits, softmax/class selection,
branch maxima and margin. Count actual pairs and forwards, including any failed
attempt. A deterministic native unscorable condition is retained and reconciled
under the common fail-closed rule; unexpected asymmetry or nonfinite/missing
scores stops the stage for technical review. Do not silently delete rows or
introduce policy-specific eligibility. This is the existing paired GbV adaptation,
not an E0/E1 margin renamed as TrustMargin or a new published baseline variant.

## C4c. Frozen policies, independent checks and prelabel seal

The five-head artifact is MODELS.json under empirical_fixed_panel_v1, whose
manifest is 4579e712f5ad513d7a9ca1229809432f766c05d5d9033b9053563c076629a619.
Use its saved preprocessing, coefficients and Platt calibrators, with no refit
or recalibration. Keep the original V2 bundle and score_unseen definition;
its training-reference empirical CDFs stay fixed and are not rebuilt from this
fresh batch. Raw HGB, raw paired GbV and Keep complete the nine-policy panel.

Retain all 18,000 rows in N_all. Freeze one common eligibility mask across all
scored policies, with complete reason counts and reconciliation. Apply the same
global K=900 to each scored policy: descending frozen score, then canonical
(dataset,retriever,sample_id), min(K,eligible_count); Keep always keeps. No score
threshold, stratum quota, model selection, budget optimization or separate
acquisition is permitted. All policies and all failures remain reportable.

Before any real scoring, require committed adapters/validators, immutable input
and model/source/environment manifests, synthetic renderer/feature/cache/
resolver/eligibility/tie tests, saved-model deserialization checks and separate
invented-only GPU preflights for the scoring paths. Do not compete with the
ongoing C3 GPU pass. Run stages into separate single-use namespaces; never write
to old sources, model files, caches or sealed experiments. Explicit read/write/
import/network guards must deny raw benchmarks, outcomes, fitting and generation.

The independent validator must cover all trace/evidence/token/feature/score/
eligibility/action bindings, numerical head replay and policy budgets. Label its
evidence precisely: saved neural witnesses and independent downstream arithmetic
are not a second full neural forward replay. Any additional bounded neural
replay must have a prospective fixed design before its first scoring call; it
cannot be chosen from a favorable or failed output subset after inspection.
Accept and seal the complete common prelabel ledger before a separate Gold
mapping process. D's analysis still requires an executable freeze and independent
checks; no single passing interval makes the project Submission Ready.

P0 remains valid fresh replication and contribution evidence, historical training
provenance limits, and eventual CAS journal/institution qualification. P1 covers
baseline fidelity, full acquisition/scoring cost and release reproducibility.
P2 adds no model, feature, seed or budget search. Fit total stays 178; both
historical advancement failures and the absence of a cleared novel candidate stay
unchanged. The user's journal scope remains undecided without blocking experiments.
