# Client acceptance: relocated native assembly only

2026-09-11. **CAS Q2 STATUS: NOT READY.** The first delivery gate passes;
full empirical delivery and neural replay remain unaccepted.

The version-2 path adapter and version-4 probe assemble the authenticated native
definitions in two fresh isolated Python processes, including a directory with
spaces and Chinese characters. Each root contains 83 source/configuration,
receipt and audit files, 1,103,522 bytes. Every copy and its original input remain
unchanged, and all 21,267 files in the separate neural environment/targets pass
the post-run byte and exact-file-set check. The original dirty checkout, sealed
controls and frozen source bindings were also rechecked unchanged.

Each process matches all 46 prior retrieval/reader AST records and the existing
generation-boundary record, plus all 34 prior base/V2 scoring AST records.
Expected identities come from the already accepted native CPU receipts, not
newly invented reference digests. The adapter explicitly records 21 Path-global
bindings. Retrieval's absent import helper remains absent; only runtime and
V3's existing import helpers are replaced. All other support global objects
remain identical within their respective process. Original scientific files,
metadata, model revisions, constants and accepted extraction boundaries remain
unchanged. This is a separate disclosed IO adapter, not an unadapted historical
controller replay.

There are no model loads, forwards, fits or fresh outcome reads. No old-project
content access is observed. The scientific fit total remains 178. Before each
strict assembly guard, an audited Windows metadata bootstrap permits exactly
one authenticated System32 `cmd /c ver` and its stock NUL handle. During assembly
subprocesses remain forbidden. Each sklearn optional PyArrow availability probe
is denied with its expected ModuleNotFoundError; the target is not added or
imported. No unexpected blocked event occurs in either accepted process.

The 13 version-2 path tests pass, including hash-before-execution, traversal,
collision, hardlink and unchanged-function boundaries. The earlier full
engineering run had 151 tests, 149 pass and two Windows capability skips; that
suite was not rerun or represented as newly passing here.

## Retained failures and authority

Three preceding runs remain failed, separately sealed and included in the
private kit: the erroneous universal import-helper assumption; the blocked
CPython NUL/platform query; and the optional PyArrow probe's wrong exception
semantics. Read the prospective [path design](EMPIRICAL_DELIVERY_PATH_DESIGN.md),
[import topology correction](EMPIRICAL_DELIVERY_IMPORT_REFINEMENT.md),
[Windows bootstrap refinement](EMPIRICAL_DELIVERY_BOOTSTRAP_REFINEMENT.md) and
[optional-import refinement](EMPIRICAL_DELIVERY_OPTIONAL_IMPORT_REFINEMENT.md).
No original source/hash was changed to remove a failure.

Accepted implementation commit: `143ba2d20ab35dff19cac42fd4975e8d89e82fef`.
Accepted manifest:
`cb2efd72fe1e67c0dc02678d1f6e4c24cf04daf3d12755f9a736e889319057aa`.
[Machine-readable results](EMPIRICAL_DELIVERY_ASSEMBLY_RESULTS.json) contain
all failure pins, the two transport pins, test pin and private-kit digest.
The private kit has 433 round-trip-verified members, 1,496,967 bytes, including
all four run namespaces and audit controllers. It contains no model weights,
datasets or fresh answers and was not uploaded. Use the archived v2 adapter/v4
probe and frozen commands for this narrow gate; do not rerun accepted stages
merely to regenerate the same evidence.

## Remaining scientific and delivery gates

C3 remains its original live PID 55688. At 10:49:57 UTC, sequential byte counts
show 9,801 canonical pairs and 29,404 generation receipts. The snapshot is
non-atomic, not completion or runtime acceptance. All 41 bound source/config
files remain unchanged. Do not switch its environment or path bindings.

P0: complete C3, its fixed 180-trace replay and independent acceptance; then
actual C4/full prelabel, cost and the four D stages, followed by contribution
review. The leading rejection risk is still unestablished contribution after
both historical advancement failures. Fresh empirical results and seven
historical per-estimator fit-time receipts remain missing. The fixed HGB/GbV
comparisons and disclosed paired-verifier adaptation do not establish novelty.

P1: close every scientific data/model and completed-output dependency, preserve
failed records, explicitly resolve C4's package/root and absolute predecessor
checks, and prospectively validate relocated pipeline execution after the
original stages pass. Assembly does not establish those gates, complete
contamination control, another host/OS reproduction or a deployment candidate.
Keep missing canonical cost observations explicit. P2: no additional
model/feature/seed/budget search. CAS year, institution rule and journal remain
user-undecided; no manuscript is authorized.
