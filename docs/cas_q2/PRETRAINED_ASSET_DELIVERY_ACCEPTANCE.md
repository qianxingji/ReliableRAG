# Client acceptance: exact pretrained assets delivered and restored

2026-09-11. **CAS Q2 STATUS: NOT READY.** The three pretrained snapshots now
have a verified private offline archive and an actual independent restoration.
This closes their payload-delivery gap, not full neural-pipeline reproduction.

The archive contains all 22 files / 7,494,996,533 payload bytes: six BGE, nine
Qwen and seven GbV NLI files at their already frozen revisions. Configurations,
tokenizers, Qwen's shard index and NLI's existing README retain their original
bytes and relative locations. Original acquisition/scoring audits, rather than
newly generated expected hashes, independently authenticate every payload.

A separate isolated base-Python process reauthenticated both embedded audits,
reconstructed the exact file lock, validated all 30 archive members and actually
restored the assets to `E:/paper/ReliableRAG-pretrained-restoration-v1`. Every
restored byte matches the original audit. Qwen's 434 index entries reference
exactly the two included shard files. Tensor bodies were never deserialized.
The independent validator observed zero old-project content reads. Both process
guards report no blocked event and no model load, forward, fit or fresh Gold
access. Only the one declared validator subprocess was started by the writer.

After restoration, all 22 original assets, all 22 restored files and seven
source/control/metadata inputs were rechecked unchanged. This adds real asset
transport and extraction evidence; no engineering-suite rerun, CUDA inference
or new numerical experiment is claimed. Total scientific fits remain 178.

Implementation: `ba2d2d569262f5a9e0e9d7e5889212bad15b0640`.
Acceptance manifest:
`d8b867e17d74b1dce59e2ec5fb3b4af38b9991b83fbfe3fdd2d403d6d61f36cc`.
Read the [prospective contract](PRETRAINED_ASSET_DELIVERY_CONTRACT.md),
[file lock](PRETRAINED_ASSET_LOCK.json),
[machine-readable results](PRETRAINED_ASSET_DELIVERY_RESULTS.json) and
[offline restoration runbook](PRETRAINED_ASSET_OFFLINE_RUNBOOK.md).
The private two-part delivery consists of the asset ZIP and a small evidence
ZIP containing its freeze, validator and acceptance records. Their sizes and
digests are in the results. Neither part was uploaded.

The previously accepted numerical replay, package/CPU environment and two-root
source assembly remain separate evidence. Preserve their earlier failed runs,
all historical failures/seeds and the original NumPy RECORD discrepancy. No
active loader, original model asset or C3 input has been replaced with this
restoration. Do not repeat accepted delivery merely to regenerate a receipt.

P0: finish the original C3 process, fixed replay and independent acceptance;
then actual C4/full prelabel, cost and four D stages, followed by contribution
review. Fresh results are still missing. Both historical attribution gates
failed; HGB/GbV remain fixed comparisons, not proof of a novel contribution.
Seven original fit-time ID/matrix receipts remain missing.

P1: complete dataset, learned-policy and accepted-output dependency closure;
resolve the remaining C4 package/root and absolute predecessor bindings; then
validate a complete relocated pipeline under its own prospective gate. The
pretrained kit excludes those other dependencies and does not establish a
deployment candidate, contamination control or another-host/OS reproduction.
P2: no added model, feature, seed or budget search. CAS year, institutional rule
and journal remain user-undecided. No manuscript is authorized.
