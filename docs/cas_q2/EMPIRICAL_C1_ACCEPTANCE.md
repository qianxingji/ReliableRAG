# Client Research Lead acceptance: empirical stage C1

CAS Q2 STATUS: NOT READY.

Accept the guarded native projection and pool build at commit
c3651dbd622224d9dd112a72ae9ed78b4555eb0f, under
EMPIRICAL_C1_EXECUTION_CONTRACT.md and EMPIRICAL_C1_WINDOWS_CORRECTION.md.
All 6,000 selected questions are present in their frozen order. The separate
validator passed 60,818 checks, reconstructing every runtime row and every one
of the 54,716 deduplicated pool documents from the safe native projections.

Pool manifest SHA-256: 4b654f0d12eaf6a9bc08e4522932cffb3fea1845eb9bbcfb2d7c2fab3b87ab98.
See EMPIRICAL_C1_RESULTS.json for dataset counts and the preserved failure.
The first v1 attempt failed before any projection while collecting Windows
metadata. Its records and source remain unchanged. v2 changed metadata collection
and output routing only; neither selected IDs nor scientific parameters changed.

Independent validation does not import the native projector or builder. Its
derivation begins with the field-restricted projections, not a second independent
raw-source parser. Source hashes, the native ASTs, predecode scalar guard and
complete source structure preflight provide the raw-projection evidence.

The next-stage read-only input inventory passed: 44 files / 6,623,098,883 bytes,
including pinned Qwen/BGE snapshots and prompts/sources. This is availability
and byte/version evidence only; no model was loaded and no forward pass ran.
GPU compatibility and the new retrieval/runtime executable adapters are not
accepted yet. No fresh retrieval, generation, scoring, actions or Gold mapping
has occurred. Scientific fit total stays 178. The combined engineering suite
ran 37 tests: 35 passed, 2 Windows filesystem-capability skips.

C1 acceptance permits C2 engineering/preflight preparation. Before acquisition,
freeze the complete C2 model/prompt/source/environment/cohort/pool bindings and
accept the native adapters. Before outcome access, complete the independent
prelabel audit specified in EMPIRICAL_REPLICATION_PROTOCOL_V1.md.

P0 remains actual fresh replication, evidenced contribution, missing original
upstream fit-time receipts, and journal/institutional CAS qualification. The user
confirmed journal scope is undecided; it does not block experiments. P1 is
comparison/cost/release fidelity and contamination-scope assessment. P2 adds no
automatic feature/model/seed/budget search. Both advancement failures remain.
