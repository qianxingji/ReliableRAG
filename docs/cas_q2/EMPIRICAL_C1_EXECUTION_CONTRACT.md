# Research Lead stage C1 execution contract

Effective 2026-09-11, before any fresh runtime projection. CAS Q2 STATUS: NOT READY.

This prospectively narrows the engineering sequencing in section C of
EMPIRICAL_REPLICATION_PROTOCOL_V1.md: C1 prepares only the selected runtime
questions and their shared dataset pools. Its complete executable input freeze
must precede projection. Model/prompt/environment freezes for acquisition remain
mandatory before C2 retrieval, generation or scoring; they are not prerequisites
for this model-free preparation. The original protocol is preserved. This is an
explicit staging amendment, not a scientific parameter, cohort or outcome change.

Use the independently sealed 6,000-question cohort in its exact order, with the
sample-size corrigendum. All 2,000 contributors per dataset remain. Authenticate
the original pool namespace against the accepted upstream audit, native helper,
all extracted native AST definitions, raw sources and the accepted value-blind
censuses. Apply the original census structural assertions, including required
scalar types and paragraph shapes. Pin interpreter, command, source commit,
all wrapper/validator sources, this contract, the scientific protocol and input
records before projection. Preserve all previous experiment namespaces.

Use the original projectors and runtime adapters, without changing their ASTs,
and the original pooled-corpus builder. Only cohort input and output paths change.
Hotpot uses the authenticated complete gold-free runtime projection. The native
scalar guard permits keys, identifiers, questions and document fields only;
answer/support/decomposition values must be skipped before decoding. An exact
read allowlist and output-only write guard apply during projection. No network,
subprocess, model imports, retrieval index, inference, score, action or evaluation.

Single-use namespace: outputs/cas_q2/empirical_candidate_pool_v1. Save selected
source projections containing allowed fields only, native RuntimeExample rows,
native pool rows, counts/fingerprints, scalar path counters and complete input
digests. All benchmark-derived text remains private and Git-ignored. Preserve
any partial failure without overwriting or changing input hashes.

A separate process imports neither executor nor native projector/builder. It
verifies the complete execution manifest and frozen input/source hashes; exactly
reconstructs all 6,000 runtime rows from the safe projected source rows; and
independently reconstructs every pool document, identity, first-occurrence order,
deduplication count and corpus fingerprint. It checks complete cohort coverage,
canonical serialization, exact field allowlists and zero downstream calls.
This verifies runtime/pool derivation from guarded native projections; it does
not claim a second independent parser of the original raw source bytes.

Before execution, run synthetic tests of forbidden scalar decoding, unselected
row exclusion, duplicate/schema failure and native-versus-independent corpus
semantics. There are no synthetic or scientific fits. Acceptance of C1 clears
only downstream adapter/input-freeze preparation. Fresh labels stay closed until
the separate complete prelabel audit required by the scientific design passes.
