# C3 V3.2 final authenticity acceptance

Date: 2026-09-12  
Reviewer role/model: client Research Project Lead, Astra xhigh  
Decision: **ACCEPTED_COMPLETE_CURRENT_C3_V3_2**  
CAS Q2 STATUS: **NOT READY**

## Accepted scope

The unique full V3.2 launch completed the unchanged V3.1 scientific validator
through the standard-library isolated bootstrap. The final runtime namespace is
sealed by manifest
`dc2905224798b07302db8950f68db228de1faff9042c44dbaee7111ba107878f`.
Its independent report is
`58500b1d9002ec9af9a8077349e4a70c8584321cca06db005211602aecfa6395`.

The client audit independently rehashed the 70-file producer task, reopened all
71 private-archive members, rehashed all 82 files in the final runtime manifest,
and reconstructed the pre-binding result hash
`22210f4a7c33077ecc9084532f306bc8c5304bb0f9c53c4b2f1d9c9fbf7f7c29`.
It streamed the four canonical and replay ledgers independently, recomputed
18,000 canonical signatures and 180 replay signatures, and found exact replay
membership, order and signature equality.

The accepted execution checked 54,540 generations, 834,803 generated token IDs
and the complete tokenizer vocabulary 38 times. It recorded no failed generation
check, model forward, scientific fit or fresh Gold materialization. All 30,912
files in the frozen environment inventory, 66 final controls, the NumPy OpenBLAS
0.3.29 12-thread target and the four frozen auxiliary thread pools were rechecked.
The V1, V2, V3-fixture and V3.1-launch failures remain preserved and failed.

The first client-audit controller attempt is also preserved under manifest
`61a54dba43ffb8ca8fc0ee629668ab2fa65b42315b59443a307ca9b40683426f`.
It stopped on an audit-only field-mapping `KeyError` after environment rehashing;
it did not change or reject the scientific artifact. V2 remains a literal
one-thread numerical failure. The final acceptance does not rewrite it or infer
the unknown historical acquisition thread count.

## Downstream gate

The reviewer actually called
`stage_prerequisites("gpu_preflight", {"runtime": <accepted manifest>})`.
It returned 204 verified predecessor paths without mutation. C3 is therefore
accepted for candidate-pair acquisition and C4 is authorized under the already
frozen C4 execution contract. The C3 neural generation and validation must not
be rerun.

This acceptance establishes artifact integrity, complete saved-field validation
and exact bounded replay. It does not assess current answer quality, choose a
novel method, fit a final deployment model or establish submission readiness.

## Remaining rejection risks and priorities

- **P0:** execute and independently accept C4 GPU preflight, base, GbV, policies
  and complete prelabel scoring; then run the authorized cost and D stages.
- **P0:** complete fresh label-blind evaluation of the already frozen
  `GBV_ONLY_R` and `HGB_GBV_R` controls, then audit contribution on the opened
  outcomes before choosing any final candidate or confirmation design.
- **P1:** recover or clearly delimit the seven missing historical fit-time
  receipts and the unknown historical BLAS thread count in reproducibility claims.
- **P2:** resolve CAS partition year, institutional category rule and target
  journal before the final Submission Ready audit.
