# C3 V3.1 invented-only gate: client audit

Research Lead acceptance, Astra xhigh, 2026-09-12 Asia/Shanghai.  
**CAS Q2 STATUS: NOT READY.** The V3.1 invented-only gate is accepted. This does
not accept the full C3 validator, permit C4 or establish fresh answer quality.

## Decision

The gate is accepted as `ACCEPTED_C3_VALIDATION_V3_1_INVENTED_ONLY`. It tests
new V3.1 sources created under the prospective
[environment amendment](C3_VALIDATION_V3_1_CONTRACT_AMENDMENT.md), while retaining
the original V3 files and both literal failed V3 fixture attempts.

The main process passes all nine dataset/retriever strata, eight exact branch
rejections, inherited helper-scope authentication, tokenizer-length checks and
original finalization/no-overwrite behavior. Three invented generation receipts,
11 generated token IDs and two complete-vocabulary checks reconcile. The
separate negative process rejects all 16 prescribed environment cases, including
missing or duplicate NumPy target pools, an auxiliary pool at start, missing,
duplicate or unknown end auxiliaries, identity changes and DLL-byte changes.

## Environment and integrity

At start, the exact NumPy 2.2.6 OpenBLAS 0.3.29 SkylakeX pthreads DLL is the sole
pool and reports 12 threads. At finalization it is byte- and identity-exact, and
the remaining pool set is exactly the four-entry auxiliary allowlist frozen by
the independent anomaly audit: two PyTorch OpenMP DLLs, sklearn `vcomp140.dll`
and SciPy OpenBLAS 0.3.28. Each auxiliary API, implementation, thread count,
version, threading layer, architecture, absolute path, size and SHA-256 matches.

The fixture task manifest is
`43ac159a7776017e257e158e955be42777cff7f1f9e7e4b99526b28d9fb807fc`.
The client audit reopened and rehashed all 38 members of its 71,997-byte private
ZIP, whose SHA-256 is
`a734f071783ac0f26018bcfe7a68b62a14d07707372501daa23e41cb03f59be7`.
The client audit manifest is
`dddb9a4ae72c7697e84d2ff0e0688ebfb82ff5064707842506dcea39c2004484`.
All 87 inputs, 21 controls and 30,823 original environment files remain unchanged.
No current runtime payload, Gold value, neural forward or fit was used; total
takeover fits remain 185. Private artifacts remain local.

The two failed V3 fixture manifests remain
`c6a07d0fc41df2d1c98f2a0c438f20789b4aeb46664606436edd5fd83403a365`
and `7417903ee4a3a3f8c4a80d8fdb56539697687784c4d11c6bfee4f9f2ac44b936`.
The first counted all threadpool entries as BLAS; the second revealed the real
SciPy auxiliary pool. Neither failure read current payloads or performed model
or fit work.

## Next gate and claim boundary

The accepted fixture permits the parent contract's preservation step. Before
one full V3.1 run, archive and independently verify the V2 failure, then make the
single no-overwrite same-volume relocation specified in the V3 contract and
seal its sibling namespace. The full run remains unauthorized until that evidence
passes. V1, V2, both neural passes, the arithmetic census and these fixtures must
not be repeated.

Accepted evidence is limited to the V3.1 adapter and its execution-environment
boundary. Historical acquisition thread count remains unknown. Complete C3,
fresh empirical scoring, contribution support and Submission Ready status remain
unestablished.
