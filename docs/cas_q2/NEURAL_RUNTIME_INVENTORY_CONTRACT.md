# Full empirical-runtime dependency and path inventory

Research Lead decision, 2026-09-11, before executing the new inventory.
CAS Q2 STATUS: NOT READY.

The accepted fresh numerical environment covers original saved-parameter replay,
not C3 generation, C4 neural scoring or D's source readers. Establish an exact
current dependency/asset inventory for that remaining reproduction requirement.
Do not install or change packages, alter frozen paths/manifests/source, launch
models, run neural preflights or compete for the live C3 GPU in this stage.

## Exact scope

Inventory all currently installed distributions in the original Python environment,
plus the separately targeted SentencePiece 0.2.1 package used before C4 imports
and PyArrow 20.0.0 used by the original selected-reference reader. Preserve their
package metadata, requirements, wheel tags and RECORD declarations. Hash every
declared installed file within its authorized environment/package root; verify
every nonempty RECORD digest/size and record missing/unhashed entries separately.
Do not treat an installer-authored RECORD as independent historical provenance.

Evaluate non-extra dependency markers against this actual Windows/CPython target,
retain the dependency graph and report missing/conflicting dependencies. Extras
and inactive platform branches are not silently included or declared tested.
Current installed-but-unused packages may be listed as such; this is a complete
current-environment inventory, not a claimed minimal dependency set.

Authenticate Qwen, BGE and NLI assets against the already accepted acquisition/
scoring inventory manifests. Recheck their exact files, revision paths, digests
and sizes as opaque bytes. Include the existing externally targeted reader
package records, source/phase configs and frozen inputs needed to explain the
loaders. Decode only authenticated inventory/config metadata and source code,
never new answers, score rows, reference values or fresh Gold. Do not deserialize
model weights, load estimators/tokenizers or invoke scientific functions.

Identify the active literal roots in the original runtime/retrieval/scoring
support modules and the original-root equality check in the current C4 source
bridge. Count/classify absolute path records in accepted C3/C4/D freezes. Keep
all original strings and bytes unchanged; this stage specifies no path rewrite.
Capture Python/base interpreter facts and read-only GPU driver/device facts;
do not initialize CUDA through Torch or load GPU kernels.

## Authenticity and acceptance

Bind the inventory script, contract, exact predecessor manifests, metadata and
file records before final acceptance; exclusive new namespace only. Preserve
all failures/partial results. Rehash inventoried files after collection and
verify fixed upstream anchors unchanged. A PASS means current installed-file,
dependency and model-asset inventory only, not another neural run, a portable
package, a clean neural installation or an original training reconstruction.

Distinguish current observations from earlier evidence: original runtime metadata
names eight package versions and original scoring metadata names six. Additional
leaf dependencies first authenticated now are not retroactively asserted to have
been pinned at the earlier generation/training time. Complete official-wheel
acquisition, clean neural installation and a reviewed path strategy remain
separate work after this inventory exposes the exact requirements.

C3 remains the original live process. This CPU/IO inventory coexists with it;
observed C3 elapsed time is shared-host wall time, not a dedicated latency
benchmark. Do not rerun inference to replace missing timing/peak measurements.
Total scientific fits remain 178, new neural forwards and fresh Gold reads zero.
The failed advancement rules, missing fit-time receipts and unfinished fresh
result/contribution review remain unchanged.
