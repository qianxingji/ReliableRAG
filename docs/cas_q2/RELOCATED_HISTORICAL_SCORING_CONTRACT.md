# Full historical feature and saved-score replay in restored roots

Research Lead design, 2026-09-11. CAS Q2 STATUS: NOT READY.

The accepted static delivery now contains the original feature-pair, likelihood,
semantic-binding, ten-score and V2 action ledgers. Use these to verify the actual
restored upstream scoring path, beyond the already accepted source-only assembly.
This is a new full historical CPU replay, not another invented-only preflight and
not the already completed top-level ROA saved-parameter replay.

## Frozen scope and authority

Use the existing three roots at `E:/paper/ReliableRAG-static-roots-v1`, without
adding files to or rewriting them. Authenticate needed files through the accepted
static graph manifest `aa5c3da7ed764405e92602b64303984931a798abf2aee3e64c5d56ae34e892ea`
and delivery acceptance `54b44ed473665264a366ee0bb24e5a205b43c0bb012799fdd654592c620a4ff6`.
Use the accepted fresh neural package environment and unchanged v2 source adapter.
Exact source/config records, selected file roles, environment file-list authority,
controller bytes and command must be frozen before any scientific payload decode.
Guard controller bootstrap and isolate the fresh interpreter from old projects.

The historical parent is `outputs/daa_v2_fresh_v1/prelabel_seal_v3`, with the
original `runtime_branch_freeze/canonical_branches.jsonl` and
`prelabel_seal_v2/binding/canonical_metadata_binding.jsonl` sidecar. The already
authenticated parent seals declare 13,500 traces, 4,500 question groups, nine
1,500-trace strata and 3,202 eligible feature pairs. Keep the other 10,298 traces
with their original forced-KEEP reasons. Do not change the row universe.

Rebuild every eligible historical pair with the unchanged `native_base.restore_trace`
and `state_symmetric.build_pair_record`, using the archived answer-semantic values
and four saved likelihood cells. Compare all fields to the archived feature pair:
identities/schema exact, numeric values at absolute tolerance 1e-12, matching the
original parent independent-validation tolerance. Do not run BGE, Qwen or GbV.

Load exactly seven original mars_full estimators and the original five-fold V2
bundle through their original assembled class identities. Authenticate every
serialized input before loading, verify feature/class order and original saved
constants. Call the unchanged original `native_base.base_scores` on every eligible
rebuilt pair. Compare all ten outputs against the complete archived score ledger;
ineligible rows keep ten nulls. The original V2 `score_unseen` receives the replayed
ten-score matrix, in its original canonical key order. Compare every eligible V2
score and exact HGB/V2 actions to the archived decisions: K=675 of N=13,500,
descending score followed by (dataset, retriever, sample_id). No threshold search,
new fit, seed, calibration or branch generation is allowed. Save all per-row replay
values and discrepancies in a new private namespace, including a failed run.

The five current empirical parameter heads may be authenticated/loaded as metadata
through the unchanged `saved_models` function; this gate does not claim fresh panel
predictions or full C4 replay. Current empirical Gold, scores and live C3 ledgers
are excluded. Historical inputs here contain generated answers and prelabel numeric
features/actions, not outcome/Gold labels. This gate permits their exact decoding
only after the prospective freeze; raw dataset/reference files remain opaque.

## IO and package adaptation

Reuse the accepted v2 adapter without editing it. Record its eight v3-support Path
bindings and authenticated import helper; keep all scientific bodies and old
metadata bytes unchanged. For the archived `empirical_scoring_io.saved_models`
function, compile only its original top-level function AST into a separate module
with explicit `load`, `require`, original `FIELDS`, and physical `PANEL` bindings.
Record its full source hash, selected AST hash and injected IO globals. This is
disclosed controller adaptation, not an assertion that the complete old C4 CLI
runs unchanged. Its internal scientific checks remain executable and unchanged.

Reuse the previously accepted narrow Windows platform bootstrap and absent optional
PyArrow behavior: one authenticated cmd /c ver and stock NUL handle before the
scientific phase; exactly the optional sklearn.utils.fixes probe raises the normal
ModuleNotFoundError. No package installation, target-path injection, network,
later subprocess, Torch/Transformers import or CUDA use. One CPU thread. Deny
fit/partial_fit/fit_transform, pretrained construction and any non-allowlisted
scientific file read; forbid writes outside the new output directory.

## Independent acceptance and limits

A separate standard-library validator authenticates producer freeze/receipts and
all referenced historical ledgers, compares every saved replay row and numeric
field to the parent records, independently derives HGB/V2 top-K decisions and
checks all 27,000 action comparisons. It must not import the producer, load models
or read old-project files. Report exact coverage, maximum errors, retained nulls,
all mismatches, imported-module roots and guard counters. Rehash all used restored
inputs, original source witnesses and all 21,267 accepted environment/target files
before and after. A count-only/fixture PASS cannot satisfy this contract.

Preserve any failure and diagnose before a prospective versioned correction. No
hash, tolerance, seed, scientific function, cache or archived output may be changed
to manufacture success. No accepted results may be silently overwritten.

This verifies the historical CPU feature/saved-estimator/decision path in restored
roots. It does not retrain the seven estimators, independently reimplement their
trees, reproduce neural cells, establish fresh quality/contribution, satisfy all
future predecessor bindings, or prove another-host/OS reproduction. Seven missing
fit-time receipts remain missing. P0 remains original C3/replay/acceptance, actual
C4/prelabel, cost/D and contribution review. P1 continues full relocated execution
and completed-stage closure. P2 adds no model/feature/seed/budget search.
