# P0-G/H/I first-level nested-archive closure-evidence census

Decision: **PASS_BOUNDED_FIRST_LEVEL_NESTED_ARCHIVE_CENSUS_NO_RECOVERY**.

**CAS Q3 STATUS: NOT READY.**

On 2026-09-14, a read-only recursive follow-up covered ZIP-valued members in
the same original workspace, preserved private historical root and old
repository ZIP used by the earlier external-evidence censuses. It found four
first-level nested ZIP archives among 39 top-level ZIPs and temporarily
materialized all four by exact bytes: 269,975,228 bytes in total. Their parent
and member SHA-256, size and CRC32 identifiers are retained in the machine
receipt. All temporary archive files were removed.

The four nested archives contain 301 direct members and no deeper ZIP-valued
member. The expanded text pass scanned all 234 recognized text members,
including six members above the earlier 5,000,000-byte boundary, for a total of
858,962,287 bytes. It found zero target-evidence marker and produced zero skip
or read error. The expanded suffix set includes BibTeX, XML and log files that
were outside the first text census.

The nested archives also contain 32 PDF occurrences representing 14 unique
hashes within this nested scope. Poppler extracted every PDF and searched
323,181 text bytes. Every unique PDF has at least 20 non-whitespace extracted
text bytes; all 14 attachment inventories completed and found zero embedded
attachment. There was no marker match, skip or parser error. A strict member-
header pass found only the 32 expected PDF signatures and no extension
mismatch, OLE/RTF container or deeper ZIP.

This closes only the earlier first-level nested-ZIP omission. It does not prove
absence outside the three roots, inspect page images with OCR, or cover an
ordinary file whose extension hides an otherwise unsupported container. It
does not independently certify authority, close P0-G/H/I, or authorize
submission or distribution. No scanned file was executed, and no model
forward, scientific fit or scientific payload interpretation occurred.
