# Mistral test answer-semantics input-graph amendment

Date: 2026-09-17. **CAS Q3 STATUS: NOT READY.**

This prospective amendment was committed before any formal test execution. It
changes no trace, answer, embedding mode, batch, vector format, BGE revision,
semantic formula or endpoint. Mistral, NLI, Gold, outcomes, fitting and tuning
remain forbidden in this stage.

## Gap

The answer-semantics producer already froze every member of its four accepted
test predecessor namespaces, all validation receipts, the test trace and input
freeze, seven BGE provenance paths and direct controls. The independent
tokenizer/vector validator reverified those sources but required only three
named prerequisites to occur in the frozen set. It did not independently prove
exact equality of the complete path set.

## Prospective correction

Before reading answer receipts or constructing the tokenizer, the validator now
independently reconstructs and rehashes:

1. the selected test trace and every input-freeze member;
2. every member of accepted a0, repair, a1/likelihood and witness namespaces,
   plus all four independent validation receipts;
3. the pinned BGE preflight and six model files; and
4. the producer, validator, acquisition/scoring helpers, imported development
   and test a0 helpers, a0 validator helper, canonical reader runtime, original
   runtime/retrieval files, Python executable, frozen test protocol and this
   amendment.

Exact set equality with the producer freeze is required before the 2,250-batch
audit. The producer now also records the imported validator helper, Python
executable and this amendment. Existing raw-vector, tokenizer-field, journal,
normalization and dot-product checks remain unchanged.

Six contract tests cover exact accounting, vector normalization and dot-product
guards, witness/forbidden-access gates, validator independence, exact input
closure and unexpected-file rejection. No model load, model forward, test
semantic vector, Gold read, outcome read or scientific fit was performed while
making or testing this amendment. The complete Mistral suite passes 180 tests.
